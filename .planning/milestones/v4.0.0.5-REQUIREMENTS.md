# Requirements Archive: v4.0.0.5 — Agent DTO Reorganization

**Archived:** 2026-05-05
**Milestone:** v4.0.0.5 — Agent DTO Reorganization
**Status:** ✅ SHIPPED

---

## v4.0.0.5 Requirements — Final Status

### DTO Restructuring

- [x] **DTO-01**: Maintainer can navigate Agent-layer DTOs through folders that mirror interface boundaries — `IApiApi` DTOs now live under `Dtos/ApiApi/...`, and per-host DTOs live in roots named after their interface ownership
  - **Outcome:** ✅ Validated — Phase 12 delivered the interface-owned DTO folder layout and `12-VERIFICATION.md` marks the requirement passed.
- [x] **DTO-02**: `IApiApi`-owned DTOs live under the interface-first `Dtos/ApiApi/` root, including `Article`, `Charge`, `Coin`, `Daily`, `Relation`, `UpInfo`, `Video`, and `VipBigPoint`; the UpInfo slice uses `Dtos.ApiApi.UpInfo`
  - **Outcome:** ✅ Validated — `12-01-SUMMARY.md` and `12-UAT.md` confirm the UpInfo relocation and final `Dtos.ApiApi.*` namespaces.
- [x] **DTO-03**: `Dtos/Coin/CoinBalance.cs` moved to `Dtos/AccountApi/CoinBalance.cs` to match `IAccountApi`; namespace updated from `Dtos.Coin` to `Dtos.AccountApi`
  - **Outcome:** ✅ Validated — final layout and Phase 12 verification mark `AccountApi/CoinBalance` complete.
- [x] **DTO-04**: The Nav DTO group (`UserInfo`, `WbiImg`, `LevelInfo`, `Wallet`) lives under `Dtos/NavApi`; live heartbeat DTOs live under `Dtos/LiveTraceApi`; live room/task DTOs live under `Dtos/LiveApi`
  - **Outcome:** ✅ Validated — `12-UAT.md` checks the final Nav and live DTO ownership layout and `12-VERIFICATION.md` marks the requirement passed.
- [x] **DTO-05**: All consumers of renamed/moved DTOs (Agent interfaces, DomainServices, AppServices, tests) updated with corrected `using` directives
  - **Outcome:** ✅ Validated — `12-02-SUMMARY.md` confirms consumer migration and cleanup of stale `Dtos.UpInfo` / `NavApiApi` references.
- [x] **DTO-06**: Build 0 errors | Architecture tests 4/4 | Integration tests 7/7
  - **Outcome:** ✅ Validated — build, architecture tests, host integration tests, and UAT all passed for the completed layout.

---

## Traceability Table — Final Status

| Requirement | Phase | Status |
|-------------|-------|--------|
| DTO-01 | Phase 12 | ✅ Complete |
| DTO-02 | Phase 12 | ✅ Complete |
| DTO-03 | Phase 12 | ✅ Complete |
| DTO-04 | Phase 12 | ✅ Complete |
| DTO-05 | Phase 12 | ✅ Complete |
| DTO-06 | Phase 12 | ✅ Complete |

**Coverage:**
- v4.0.0.5 requirements: 6 total
- Fully satisfied: 6
- Partially satisfied: 0
- Unmapped: 0

---

## Future Requirements Carried Forward

The following requirements remain candidates for the next milestone and were not part of v4.0.0.5 scope:

- **TEST-04**: Maintainer can verify key Web or Blazor components with dedicated component tests
- **TEST-05**: Maintainer can enforce focused coverage thresholds for critical modules in CI
- **FLOW-05**: Maintainer can unify Console and Web configuration and startup composition paths where behavior meaningfully overlaps
- **QUAL-03**: Maintainer can remove default credential risks and similar obvious safety issues from bootstrap flows
- **QUAL-04**: Maintainer can reduce repository noise from generated outputs so searches and reviews focus on source of truth files

---

## Known Tech Debt

- Pre-existing analyzer and nullability warnings remain in the solution but are unrelated to the DTO migration itself
