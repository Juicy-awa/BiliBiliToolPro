# Milestone v4.0.0.6: Web Layer Boundary Cleanup

**Status:** ✅ SHIPPED 2026-05-05
**Phases:** 13–16
**Total Plans:** 9
**Files Changed:** 67 (+5,824 / −494)
**Timeline:** 2026-05-05 (single day)

## Overview

Refactored the Web layer so UI rendering stays thin, component code-behind stops owning workflow orchestration for the targeted slices, and the resulting seams are safer to test and evolve.

All 4 phases shipped on 2026-05-05. Dedicated bUnit component test project created with 28 passing tests. Five ArchUnit rules enforced. All 6 requirements satisfied.

## Phases

### Phase 13: Web Boundary Foundation

**Goal:** Define and introduce the first Web-facing coordination seams for the targeted Web slices, add the minimal Web test harness, and establish the pattern used by later phases.
**Requirements:** WEB-01, WEB-05
**Plans:** 2 plans

Plans:
- [x] 13-01-PLAN.md — Add the dedicated Web component-test harness and baseline Login/Admin component tests
- [x] 13-02-PLAN.md — Extract the first Login page-state seam and define auth/admin page-workflow contracts for the next phase

**Details:**
- Created `Ray.BiliBiliTool.Web.ComponentTests` project (bUnit 1.34.0 / xUnit / Microsoft.NET.Sdk.Razor)
- `ILoginPageStateFactory` / `LoginPageState` seam introduced; `Login.razor.cs` now injects factory only
- `IAdminPageWorkflow` + `AdminPasswordChangeRequest` / `AdminPasswordChangeResult` contract defined (implementation in Phase 14)
- DI: `ILoginPageStateFactory` registered as Scoped in `ServiceCollectionExtension.AddWebServices()`
- Baseline component tests: 3 for Login (no-error, error, password-toggle), 3 for Admin (username load, validation, structure)

### Phase 14: Auth And Admin UI Boundary Cleanup

**Goal:** Refactor the login and admin Web pages so validation, success-flow handling, and navigation orchestration are isolated behind the selected Web-facing seam while preserving current auth behavior.
**Requirements:** WEB-03
**Plans:** 2 plans

Plans:
- [x] 14-01-PLAN.md — Implement AdminPageWorkflow + DI registration + refactor Admin.razor.cs + Logout button markup
- [x] 14-02-PLAN.md — AdminPageWorkflow unit tests + updated AdminPageTests with FakeAdminPageWorkflow

**Details:**
- `AdminPageWorkflow` implemented; `Admin.razor.cs` now injects only `IAdminPageWorkflow` + `IAuthService`
- Success flow: MudAlert shown + conditional Logout button (no auto-navigate, no password clear per decisions D-01/D-02/D-03)
- 9 Admin component tests covering success, validation failure, service failure, username load, and structure
- `FakeAdminPageWorkflow` test double introduced for isolated component tests

### Phase 15: Scheduler UI Boundary Cleanup

**Goal:** Refactor the schedule pages and dialogs so repository-backed log polling, execution-history loading, and scheduler-event coordination are moved out of the component classes.
**Requirements:** WEB-02
**Plans:** 3 plans

Plans:
- [x] 15-01-PLAN.md — Define ISchedulerPageWorkflow + implement SchedulerPageWorkflow + DI registration + Schedules.razor.cs refactor
- [x] 15-02-PLAN.md — Define ILogsDialogWorkflow + IHistoryDialogWorkflow + implement + DI + LogsDialog + HistoryDialog refactors
- [x] 15-03-PLAN.md — Workflow unit tests (3 classes) + bUnit component tests (3 classes) in Schedules/ sub-directory

**Details:**
- `ISchedulerPageWorkflow` / `SchedulerPageWorkflow` wraps `ISchedulerService` + `IExecutionLogService`; `Schedules.razor.cs` removed direct service injection
- `ILogsDialogWorkflow` / `LogsDialogWorkflow` wraps `IExecutionLogRepository` (per D-05)
- `IHistoryDialogWorkflow` / `HistoryDialogWorkflow` wraps `IExecutionLogService` for history paging (per D-10)
- Timer callback and `ClearDisplay()` kept in `LogsDialog.razor.cs` (not workflow concern — D-06/D-08)
- 16 Schedules component tests covering all three refactored components

### Phase 16: Web Composition And Regression Verification

**Goal:** Finish the Web-layer cleanup by keeping startup and registration logic wiring-focused, then verify the milestone through build, architecture, host integration, and new Web tests.
**Requirements:** WEB-04, WEB-06
**Plans:** 2 plans

Plans:
- [x] 16-01-PLAN.md — Add ArchUnit Web.Components guardrail + extend WebStartupIntegrationTests with 6 workflow seam assertions
- [x] 16-02-PLAN.md — Full WEB-06 regression: build + arch tests + integration tests + component tests

**Details:**
- ArchUnit rule `Web_component_code_behind_classes_should_not_directly_depend_on_infrastructure` added (targets Infrastructure, not Domain — components legitimately display Domain types)
- 6 new `GetRequiredService<IXxx>()` assertions added to startup integration test
- Regression results: Build 0 errors | Arch 5/5 | Integration 7/7 | Components 28/28

---

## Milestone Summary

**Key Decisions:**
- Use `IXxxWorkflow` / `IXxxFactory` naming for Web-layer seams (clear Web ownership)
- Admin success flow: MudAlert + conditional Logout button (no auto-navigate, no password clear)
- `ILogsDialogWorkflow` wraps `IExecutionLogRepository`; `IHistoryDialogWorkflow` wraps `IExecutionLogService` (different backing services per dialog concern)
- ArchUnit rule targets `Infrastructure` not `Domain` — components legitimately reference Domain model types for display binding
- Timer callback kept in `LogsDialog.razor.cs` — not a workflow seam concern

**Accomplishments:**
- Introduced 5 explicit Web-layer coordination seams (`ILoginPageStateFactory`, `IAdminPageWorkflow`, `ISchedulerPageWorkflow`, `ILogsDialogWorkflow`, `IHistoryDialogWorkflow`)
- Created dedicated `Ray.BiliBiliTool.Web.ComponentTests` project with 28 component tests
- Added ArchUnit `Web.Components` guardrail enforcing seam usage in CI
- Extended startup integration test with 6 DI seam assertions
- All targeted component code-behind files now inject only Web-layer seams (no direct Infrastructure injection)
- Full regression suite green: 0 build errors, 5/5 arch, 7/7 integration, 28/28 components

**Tech Debt Incurred:**
- VERIFICATION.md files not produced for Phases 13–16 during execute-phase (process gap only — coverage verified via Phase 16-02 regression)
- ROADMAP plan checklists for Phases 13 and 15 showed stale `- [ ]` markers (not ticked by executor)
- Configs component subtree (`BaseConfigComponent`, `DailyJobConfig`, etc.) still injects Infrastructure/Domain types directly — out of scope for this milestone (only Login, Admin, Schedules targeted)
- Pre-existing test failure `Daily_task_multi_account_wrapper_continues_after_account_failure` deferred from earlier milestones

**Issues Deferred:**
- ARCH-04: Notification adapter/port boundary (still direct Serilog sink dependency)
- TEST-05: Focused coverage thresholds in CI
- FLOW-05: Unify Console and Web startup composition
- QUAL-03: Remove default credential risks from bootstrap flows
- QUAL-04: Reduce repository noise from generated outputs

---

_For current project status, see .planning/ROADMAP.md_
