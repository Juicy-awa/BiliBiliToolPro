---
phase: 13-web-boundary-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Ray.BiliBiliTool.sln
  - test/Ray.BiliBiliTool.Web.ComponentTests/Ray.BiliBiliTool.Web.ComponentTests.csproj
  - test/Ray.BiliBiliTool.Web.ComponentTests/_Imports.razor
  - test/Ray.BiliBiliTool.Web.ComponentTests/LoginPageTests.cs
  - test/Ray.BiliBiliTool.Web.ComponentTests/AdminPageTests.cs
autonomous: true
requirements: [WEB-05]
must_haves:
  truths:
    - "Maintainer can run focused Blazor component tests without booting the full Web host"
    - "The current Login and Admin page behaviors have source-controlled fast checks before boundary refactors begin"
    - "The Web refactor has a test harness that can be extended in Phases 14 and 15"
  artifacts:
    - path: "test/Ray.BiliBiliTool.Web.ComponentTests/Ray.BiliBiliTool.Web.ComponentTests.csproj"
      provides: "Dedicated bUnit/xUnit component-test project for the Web layer"
    - path: "test/Ray.BiliBiliTool.Web.ComponentTests/LoginPageTests.cs"
      provides: "Baseline Login page render and query-state checks"
    - path: "test/Ray.BiliBiliTool.Web.ComponentTests/AdminPageTests.cs"
      provides: "Baseline Admin page interaction and validation checks"
  key_links:
    - from: "test/Ray.BiliBiliTool.Web.ComponentTests/Ray.BiliBiliTool.Web.ComponentTests.csproj"
      to: "src/Ray.BiliBiliTool.Web/Ray.BiliBiliTool.Web.csproj"
      via: "ProjectReference"
      pattern: "ProjectReference Include=\"..\\..\\src\\Ray.BiliBiliTool.Web\\Ray.BiliBiliTool.Web.csproj\""
    - from: "test/Ray.BiliBiliTool.Web.ComponentTests/LoginPageTests.cs"
      to: "src/Ray.BiliBiliTool.Web/Components/Pages/Login.razor"
      via: "Render<Login>()"
      pattern: "Render<Login>|RenderComponent<Login>"
---

<objective>
Create the Web-layer component-test harness that will protect the Phase 13-15 refactor slices.

Purpose: Add fast feedback around targeted Razor pages before moving orchestration out of component code-behind.
Output: A dedicated bUnit/xUnit test project with baseline tests for Login and Admin pages.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/v4.0.0.6-web-layer-boundary-cleanup-RESEARCH.md
@src/Ray.BiliBiliTool.Web/Components/Pages/Login.razor
@src/Ray.BiliBiliTool.Web/Components/Pages/Login.razor.cs
@src/Ray.BiliBiliTool.Web/Components/Pages/Admin.razor
@src/Ray.BiliBiliTool.Web/Components/Pages/Admin.razor.cs
@src/Ray.BiliBiliTool.Web/Ray.BiliBiliTool.Web.csproj

<interfaces>
From src/Ray.BiliBiliTool.Web/Services/AuthService.cs:

```csharp
public interface IAuthService
{
    Task<ClaimsIdentity> LoginAsync(string username, string password);
    Task ChangePasswordAsync(string username, string currentPassword, string newPassword);
    Task<string> GetAdminUserNameAsync();
}
```

From src/Ray.BiliBiliTool.Web/Components/Pages/Login.razor.cs:

```csharp
protected override void OnInitialized()
private void TogglePasswordVisibility()
```

From src/Ray.BiliBiliTool.Web/Components/Pages/Admin.razor.cs:

```csharp
protected override async Task OnInitializedAsync()
private async Task ChangePasswordAsync()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the dedicated Web component-test harness</name>
  <files>Ray.BiliBiliTool.sln, test/Ray.BiliBiliTool.Web.ComponentTests/Ray.BiliBiliTool.Web.ComponentTests.csproj, test/Ray.BiliBiliTool.Web.ComponentTests/_Imports.razor</files>
  <behavior>
    - Test project compiles with xUnit + bUnit against the existing Web project.
    - Tests can render MudBlazor-backed components with the minimal providers and service registrations they need.
    - The project can be run independently with a single dotnet test command.
  </behavior>
  <action>Create `test/Ray.BiliBiliTool.Web.ComponentTests/` as a dedicated Razor-capable component test project. Use central package management conventions already used by the repo; do not hardcode package versions in new ad hoc files. Reference `src/Ray.BiliBiliTool.Web/Ray.BiliBiliTool.Web.csproj`, add the project to `Ray.BiliBiliTool.sln`, and add `_Imports.razor` with the bUnit, xUnit, Blazor, and DI namespaces needed for component tests. Register only the minimal test-side services/providers needed to render MudBlazor pages; do not boot the full host or duplicate host startup logic here.</action>
  <verify>
    <automated>dotnet test test/Ray.BiliBiliTool.Web.ComponentTests/Ray.BiliBiliTool.Web.ComponentTests.csproj -v minimal</automated>
  </verify>
  <done>The repository contains a standalone component-test project that builds and executes independently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze current Login and Admin page behavior with baseline component tests</name>
  <files>test/Ray.BiliBiliTool.Web.ComponentTests/LoginPageTests.cs, test/Ray.BiliBiliTool.Web.ComponentTests/AdminPageTests.cs</files>
  <behavior>
    - Login tests verify query-driven error rendering and the password-visibility interaction without posting through the real auth endpoint.
    - Admin tests verify username initialization through `IAuthService` and current client-side validation failures for empty or mismatched new passwords.
    - Tests remain focused on observable component behavior rather than exact incidental markup structure.
  </behavior>
  <action>Add focused bUnit tests for `Login.razor` and `Admin.razor`. For Login, cover the `error=true` query-string alert path and the password visibility toggle. For Admin, use a test double for `IAuthService` to verify username initialization and the two validation branches that currently short-circuit before service-side password changes. Prefer semantic assertions and rendered output checks over brittle full-markup snapshots. Do not pull scheduler components into this plan; keep the test harness small and auth-page focused so it can serve as the fast guardrail for the next phases.</action>
  <verify>
    <automated>dotnet test test/Ray.BiliBiliTool.Web.ComponentTests/Ray.BiliBiliTool.Web.ComponentTests.csproj -v minimal</automated>
  </verify>
  <done>Login/Admin baseline component tests pass and give later phases a fast regression signal for targeted Web-page behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component tests -> injected doubles | Tests simulate auth/UI dependencies and must not silently depend on the full host |
| query-string input -> Login component | Untrusted query values drive error rendering state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | T | Login/Admin component tests | mitigate | Assert behavior through public rendering and injected doubles so later refactors cannot silently change targeted auth-page outcomes |
| T-13-02 | I | Test harness service registration | accept | The harness is test-only and contains no production secrets; keep registrations minimal and local to the test project |
</threat_model>

<verification>
- `dotnet test test/Ray.BiliBiliTool.Web.ComponentTests/Ray.BiliBiliTool.Web.ComponentTests.csproj -v minimal`
</verification>

<success_criteria>
- A dedicated Web component-test project exists in source control and is wired into the solution.
- Login and Admin pages have fast baseline component tests covering current targeted behavior.
- Later Web boundary refactors can use this test suite instead of relying only on full-host integration tests.
</success_criteria>

<output>
After completion, create `.planning/phases/13-web-boundary-foundation/13-01-SUMMARY.md`
</output>