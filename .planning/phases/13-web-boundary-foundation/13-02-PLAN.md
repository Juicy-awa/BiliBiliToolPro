---
phase: 13-web-boundary-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Ray.BiliBiliTool.Web/Components/Pages/Login.razor.cs
  - src/Ray.BiliBiliTool.Web/Extensions/ServiceCollectionExtension.cs
  - src/Ray.BiliBiliTool.Web/Services/Pages/Login/LoginPageState.cs
  - src/Ray.BiliBiliTool.Web/Services/Pages/Login/ILoginPageStateFactory.cs
  - src/Ray.BiliBiliTool.Web/Services/Pages/Login/LoginPageStateFactory.cs
  - src/Ray.BiliBiliTool.Web/Services/Pages/Admin/IAdminPageWorkflow.cs
  - src/Ray.BiliBiliTool.Web/Services/Pages/Admin/AdminPasswordChangeResult.cs
autonomous: true
requirements: [WEB-01]
must_haves:
  truths:
    - "The Login page no longer owns query parsing and initial state derivation directly in component code-behind"
    - "The Web layer has an explicit auth/admin page-workflow contract that later phases can implement and consume"
    - "The first Web-boundary seam is concrete, DI-registered, and ready for follow-up refactors"
  artifacts:
    - path: "src/Ray.BiliBiliTool.Web/Services/Pages/Login/ILoginPageStateFactory.cs"
      provides: "Explicit Web-facing contract for Login page initial state"
    - path: "src/Ray.BiliBiliTool.Web/Services/Pages/Login/LoginPageStateFactory.cs"
      provides: "Concrete query-state parsing service for the Login page"
    - path: "src/Ray.BiliBiliTool.Web/Services/Pages/Admin/IAdminPageWorkflow.cs"
      provides: "Auth/admin workflow contract ready for Phase 14 migration"
  key_links:
    - from: "src/Ray.BiliBiliTool.Web/Components/Pages/Login.razor.cs"
      to: "src/Ray.BiliBiliTool.Web/Services/Pages/Login/LoginPageStateFactory.cs"
      via: "Injected ILoginPageStateFactory"
      pattern: "ILoginPageStateFactory"
    - from: "src/Ray.BiliBiliTool.Web/Extensions/ServiceCollectionExtension.cs"
      to: "src/Ray.BiliBiliTool.Web/Services/Pages/Login/LoginPageStateFactory.cs"
      via: "Scoped DI registration"
      pattern: "AddScoped<ILoginPageStateFactory, LoginPageStateFactory>"
---

<objective>
Establish the first explicit Web-facing coordination seam for the auth slice and use it in the Login page.

Purpose: Give the Web layer a concrete page-state/service pattern before the larger auth/admin and scheduler cleanups begin.
Output: Login page state service, auth/admin workflow contracts, and the first migrated component using the new seam.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/v4.0.0.6-web-layer-boundary-cleanup-RESEARCH.md
@src/Ray.BiliBiliTool.Web/Components/Pages/Login.razor
@src/Ray.BiliBiliTool.Web/Components/Pages/Login.razor.cs
@src/Ray.BiliBiliTool.Web/Components/Pages/Admin.razor.cs
@src/Ray.BiliBiliTool.Web/Services/AuthService.cs
@src/Ray.BiliBiliTool.Web/Controllers/AuthController.cs
@src/Ray.BiliBiliTool.Web/Extensions/ServiceCollectionExtension.cs

<interfaces>
From src/Ray.BiliBiliTool.Web/Services/AuthService.cs:

```csharp
public interface IAuthService
{
    Task<ClaimsIdentity> LoginAsync(string username, string password);
    Task ChangePasswordAsync(string username, string currentPassword, string newPassword);
    Task<string> GetAdminUserNameAsync();
}
```

From src/Ray.BiliBiliTool.Web/Controllers/AuthController.cs:

```csharp
[HttpPost("login")]
public async Task<IActionResult> Login([FromForm] string username, [FromForm] string password, [FromForm] string? returnUrl)
```

From src/Ray.BiliBiliTool.Web/Components/Pages/Login.razor.cs:

```csharp
protected override void OnInitialized()
private string? returnUrl;
private bool _loginError = false;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract Login page initial-state logic behind a Web-facing seam</name>
  <files>src/Ray.BiliBiliTool.Web/Components/Pages/Login.razor.cs, src/Ray.BiliBiliTool.Web/Services/Pages/Login/LoginPageState.cs, src/Ray.BiliBiliTool.Web/Services/Pages/Login/ILoginPageStateFactory.cs, src/Ray.BiliBiliTool.Web/Services/Pages/Login/LoginPageStateFactory.cs</files>
  <behavior>
    - Login query parsing for `returnUrl` and `error` no longer lives directly in the component class.
    - The component consumes a narrow page-state result object and remains responsible only for display state and user interaction.
    - Existing form POST behavior through `AuthController` remains unchanged.
  </behavior>
  <action>Create a small Login page-state model and `ILoginPageStateFactory` service that encapsulates the current query-string parsing behavior. Update `Login.razor.cs` to inject the factory and assign its local fields from the returned state object instead of parsing `NavigationManager.Uri` inline. Keep `Login.razor` form structure and `/auth/login` post target unchanged; this plan is about the page-state seam, not the authentication pipeline itself.</action>
  <verify>
    <automated>dotnet build Ray.BiliBiliTool.sln --no-restore -v minimal</automated>
  </verify>
  <done>The Login page uses a dedicated Web-layer service for initial state derivation, and inline query parsing is removed from the component code-behind.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the auth/admin page-workflow contract and register the new seam in DI</name>
  <files>src/Ray.BiliBiliTool.Web/Extensions/ServiceCollectionExtension.cs, src/Ray.BiliBiliTool.Web/Services/Pages/Admin/IAdminPageWorkflow.cs, src/Ray.BiliBiliTool.Web/Services/Pages/Admin/AdminPasswordChangeResult.cs</files>
  <behavior>
    - The Web layer exposes an explicit auth/admin page-workflow contract for Phase 14 instead of leaving page orchestration shape implicit.
    - DI registration makes the Login seam available and establishes the same registration home for later page-workflow services.
    - No repository or password-hashing logic is duplicated outside `IAuthService`.
  </behavior>
  <action>Add `IAdminPageWorkflow` and an outcome model representing the current Admin password-change branches (`validation failure`, `service failure`, `success + logout required`). Keep the implementation for Phase 14; this plan only defines the contract and result shape so the next phase can migrate the component without rediscovering the API. Update `ServiceCollectionExtension.cs` to register `ILoginPageStateFactory` and reserve the same Web service-registration surface for future page-workflow services. Do not move password persistence, claims creation, or controller POST handling out of the existing auth services in this plan.</action>
  <verify>
    <automated>dotnet test test/Ray.BiliBiliTool.ArchitectureTests/Ray.BiliBiliTool.ArchitectureTests.csproj --no-build -v quiet</automated>
  </verify>
  <done>The Web layer has a concrete Login seam in DI plus explicit auth/admin workflow contracts that later phases can consume without reopening design questions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser query-string -> Login page state | Untrusted URL input controls displayed error and return-target state |
| component -> auth/admin page workflow contract | UI delegates page logic to Web-owned services rather than inline code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | T | Login page query parsing | mitigate | Centralize parsing in `ILoginPageStateFactory` so returnUrl/error handling has one audited implementation point |
| T-13-04 | E | Future Admin page workflow | mitigate | Keep password-changing authority inside `IAuthService`; the new workflow contract only models page behavior and outcomes |
</threat_model>

<verification>
- `dotnet build Ray.BiliBiliTool.sln --no-restore -v minimal`
- `dotnet test test/Ray.BiliBiliTool.ArchitectureTests/Ray.BiliBiliTool.ArchitectureTests.csproj --no-build -v quiet`
</verification>

<success_criteria>
- The Login page has its first extracted Web-facing state seam.
- The Web layer contains explicit auth/admin page-workflow contracts for the next phase.
- Phase 14 can focus on migration and behavior preservation instead of rediscovering the seam shape.
</success_criteria>

<output>
After completion, create `.planning/phases/13-web-boundary-foundation/13-02-SUMMARY.md`
</output>