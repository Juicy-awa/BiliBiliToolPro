---
phase: 06-integration-boundary-and-failure-model
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/Ray.BiliBiliTool.DomainService/LoginDomainService.cs
  - src/Ray.BiliBiliTool.DomainService/AccountDomainService.cs
  - src/Ray.BiliBiliTool.DomainService/LiveDomainService.cs
  - src/Ray.BiliBiliTool.DomainService/VideoDomainService.cs
  - src/Ray.BiliBiliTool.DomainService/ArticleDomainService.cs
  - src/Ray.BiliBiliTool.DomainService/DonateCoinDomainService.cs
  - src/Ray.BiliBiliTool.DomainService/VipBigPointDomainService.cs
autonomous: true
requirements: [FLOW-04, QUAL-01]

must_haves:
  truths:
    - "No throw new Exception(...) remains in LoginDomainService, AccountDomainService, LiveDomainService, VideoDomainService, ArticleDomainService, DonateCoinDomainService, or VipBigPointDomainService"
    - "All 14 converted throws use BiliBusinessException or BiliIntegrationException from Ray.BiliBiliTool.Domain.Exceptions"
    - "Exception messages are identical to the originals — only the exception type changes"
    - "Architecture tests still pass — no new layer violations introduced"
  artifacts:
    - path: "src/Ray.BiliBiliTool.DomainService/LoginDomainService.cs"
      provides: "Login flow with typed exceptions"
      contains: "BiliBusinessException"
    - path: "src/Ray.BiliBiliTool.DomainService/VideoDomainService.cs"
      provides: "Video flow with typed exceptions"
      contains: "BiliBusinessException"
    - path: "src/Ray.BiliBiliTool.DomainService/VipBigPointDomainService.cs"
      provides: "VipBigPoint flow with typed exceptions"
      contains: "BiliBusinessException"
  key_links:
    - from: "LoginDomainService QingLong throws"
      to: "BiliIntegrationException"
      via: "lines 240, 246 (QingLong token/env failures)"
      pattern: "BiliIntegrationException"
    - from: "All API response-code throws"
      to: "BiliBusinessException"
      via: "apiResponse.Code != 0 check sites"
      pattern: "BiliBusinessException"
---

<objective>
Replace all generic `throw new Exception(...)` in seven DomainService files with typed exceptions from the BiliException hierarchy. Exception messages are preserved verbatim — only the exception type changes.

Purpose: After Plan 01 establishes the typed exception hierarchy and adds the Domain reference to DomainService.csproj, this plan converts all 14 throw sites across seven domain service files. Log behavior is unchanged (per D-04).
Output: Seven modified DomainService files with zero remaining `throw new Exception(...)` (except AuthService.cs which is excluded).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-integration-boundary-and-failure-model/06-CONTEXT.md
@.planning/phases/06-integration-boundary-and-failure-model/06-01-SUMMARY.md

<interfaces>
<!-- Classification of each throw site. Executor reads the file, finds this line, replaces the type. -->

Exception type rules (per D-03, D-05, D-06):
- API returned non-zero Code (business failure) → BiliBusinessException
- Timeout / network / QingLong external failure → BiliIntegrationException
- Cookie/input validation → BiliValidationException (handled in Plan 04)

Throw sites by file:

LoginDomainService.cs (4 throws):
  - Line ~40: `throw new Exception($"获取二维码失败：{re.ToJsonStr()}")` 
    → re.Code != 0 — API business failure → BiliBusinessException
  - Line ~102: `throw new Exception("登录超时")`
    → Login polling timed out — integration failure → BiliIntegrationException
  - Line ~240: `throw new Exception("获取青龙token失败")`
    → QingLong token fetch failed — integration failure → BiliIntegrationException (per D-06)
  - Line ~246: `throw new Exception($"查询环境变量失败：{qlEnvList.ToJsonStr()}")`
    → QingLong env query failed — integration failure → BiliIntegrationException (per D-06)

AccountDomainService.cs (1 throw):
  - Line ~38: `throw new Exception("登录失败，请检查Cookie")`
    → apiResponse.Code != 0 or !IsLogin — business failure → BiliBusinessException

LiveDomainService.cs (1 throw):
  - Line ~728: `throw new Exception(liveHomeContent?.Message)`
    → API returned non-success — business failure → BiliBusinessException

VideoDomainService.cs (2 throws):
  - Line ~70: `throw new Exception(re.Message)` → BiliBusinessException
  - Line ~91: `throw new Exception(re.Message)` → BiliBusinessException

ArticleDomainService.cs (2 throws):
  - Line ~171: `throw new Exception(re.Message)` → BiliBusinessException
  - Line ~244: `throw new Exception(re.Message)` → BiliBusinessException

DonateCoinDomainService.cs (1 throw):
  - Line ~190: `throw new Exception(errorMsg)` → BiliBusinessException

VipBigPointDomainService.cs (3 throws):
  - Line ~36:  `throw new Exception(allTasks.ToJsonStr())` → BiliBusinessException
  - Line ~117: `throw new Exception(re.ToJsonStr())` → BiliBusinessException
  - Line ~244: `throw new Exception(re.ToJsonStr())` → BiliBusinessException

Required using directive to add to each modified file:
```csharp
using Ray.BiliBiliTool.Domain.Exceptions;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert throws in LoginDomainService, AccountDomainService, LiveDomainService</name>
  <files>
    src/Ray.BiliBiliTool.DomainService/LoginDomainService.cs,
    src/Ray.BiliBiliTool.DomainService/AccountDomainService.cs,
    src/Ray.BiliBiliTool.DomainService/LiveDomainService.cs
  </files>
  <action>
    For each file:
    1. Add `using Ray.BiliBiliTool.Domain.Exceptions;` at the top.
    2. Replace each `throw new Exception(...)` with the appropriate typed exception per the classification above.
    3. Keep exception messages IDENTICAL to originals — only the exception type changes (per D-04).
    4. Do NOT change any log statements, log levels, or other code.

    Specific replacements:

    LoginDomainService.cs:
    - `throw new Exception($"获取二维码失败：{re.ToJsonStr()}")` → `throw new BiliBusinessException($"获取二维码失败：{re.ToJsonStr()}")`
    - `throw new Exception("登录超时")` → `throw new BiliIntegrationException("登录超时")`
    - `throw new Exception("获取青龙token失败")` → `throw new BiliIntegrationException("获取青龙token失败")`
    - `throw new Exception($"查询环境变量失败：{qlEnvList.ToJsonStr()}")` → `throw new BiliIntegrationException($"查询环境变量失败：{qlEnvList.ToJsonStr()}")`

    AccountDomainService.cs:
    - `throw new Exception("登录失败，请检查Cookie")` → `throw new BiliBusinessException("登录失败，请检查Cookie")`
    - Note: the trailing double semicolon `; ;` present in the original can be cleaned up.

    LiveDomainService.cs:
    - `throw new Exception(liveHomeContent?.Message)` → `throw new BiliBusinessException(liveHomeContent?.Message ?? "Live API returned failure")`
    - Note: null-coalescing the message because the original `liveHomeContent?.Message` can be null; passing null to Exception(string?) is valid but explicit fallback is cleaner.
  </action>
  <verify>
    <automated>dotnet build src/Ray.BiliBiliTool.DomainService/Ray.BiliBiliTool.DomainService.csproj --no-incremental -v q 2>&amp;1 | Select-String "error"</automated>
  </verify>
  <done>LoginDomainService, AccountDomainService, LiveDomainService have zero `throw new Exception` calls; all use BiliBusinessException or BiliIntegrationException; DomainService project builds with 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Convert throws in VideoDomainService, ArticleDomainService, DonateCoinDomainService, VipBigPointDomainService</name>
  <files>
    src/Ray.BiliBiliTool.DomainService/VideoDomainService.cs,
    src/Ray.BiliBiliTool.DomainService/ArticleDomainService.cs,
    src/Ray.BiliBiliTool.DomainService/DonateCoinDomainService.cs,
    src/Ray.BiliBiliTool.DomainService/VipBigPointDomainService.cs
  </files>
  <action>
    For each file:
    1. Add `using Ray.BiliBiliTool.Domain.Exceptions;` at the top.
    2. Replace each `throw new Exception(...)` with `throw new BiliBusinessException(...)` keeping the message identical.
    3. Do NOT change any log statements, log levels, or other code.

    All 8 throw sites in these four files are API response-code failures → BiliBusinessException:

    VideoDomainService.cs:
    - `throw new Exception(re.Message)` (×2) → `throw new BiliBusinessException(re.Message)`

    ArticleDomainService.cs:
    - `throw new Exception(re.Message)` (×2) → `throw new BiliBusinessException(re.Message)`

    DonateCoinDomainService.cs:
    - `throw new Exception(errorMsg)` → `throw new BiliBusinessException(errorMsg)`

    VipBigPointDomainService.cs:
    - `throw new Exception(allTasks.ToJsonStr())` → `throw new BiliBusinessException(allTasks.ToJsonStr())`
    - `throw new Exception(re.ToJsonStr())` (×2) → `throw new BiliBusinessException(re.ToJsonStr())`
  </action>
  <verify>
    <automated>dotnet build Ray.BiliBiliTool.sln --no-incremental -v q 2>&amp;1 | Select-String " error "</automated>
  </verify>
  <done>All four DomainService files have zero `throw new Exception` calls; all use BiliBusinessException; full solution builds with 0 errors; grep for `throw new Exception` in DomainService/ returns only AuthService.cs (excluded) — which is in Web, not DomainService.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DomainService → Bilibili API | Domain services call Agent APIs; failures now surface as typed exceptions |
| DomainService → QingLong | LoginDomainService calls QingLong; integration failures now typed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Information Disclosure | Exception messages preserved verbatim | accept | Messages are identical to existing behavior; no new sensitive data exposed in exceptions |
| T-06-08 | Denial of Service | QingLong BiliIntegrationException propagation | accept | Existing Quartz job catches exceptions via BaseJob.Execute try/catch; typed exceptions propagate cleanly |
</threat_model>

<verification>
- `dotnet build Ray.BiliBiliTool.sln` — 0 errors
- `dotnet test test/Ray.BiliBiliTool.ArchitectureTests/` — 4/4 pass
- `grep -r "throw new Exception" src/Ray.BiliBiliTool.DomainService/` — 0 matches
- `grep -r "BiliBusinessException\|BiliIntegrationException" src/Ray.BiliBiliTool.DomainService/` — ≥14 matches
</verification>

<success_criteria>
- Zero `throw new Exception(...)` in any of the seven DomainService files
- All converted throws use BiliBusinessException or BiliIntegrationException
- Exception messages unchanged; log behavior unchanged
- Solution builds clean; architecture tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-boundary-and-failure-model/06-03-SUMMARY.md`
</output>
