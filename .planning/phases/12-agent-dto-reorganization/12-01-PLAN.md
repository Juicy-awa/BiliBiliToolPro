---
phase: 12-agent-dto-reorganization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/UpInfo/GetSpaceInfoFullDto.cs
  - src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/UpInfo/GetSpaceInfoResponse.cs
  - src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/UpInfo/UpInfo.cs
  - src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/AccountApi/CoinBalance.cs
  - src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/Nav/UserInfo.cs
autonomous: true
requirements: [DTO-01, DTO-02, DTO-03, DTO-04, DTO-05]

must_haves:
  truths:
    - "Dtos/Space/ folder no longer exists"
    - "Dtos/UpInfo/ folder exists with 3 files (GetSpaceInfoFullDto, GetSpaceInfoResponse, UpInfo)"
    - "Dtos/AccountApi/ folder exists with CoinBalance.cs"
    - "Dtos/Coin/ no longer contains CoinBalance.cs"
    - "Dtos/Nav/ folder exists with UserInfo.cs"
    - "Root-level Dtos/UserInfo.cs no longer exists"
    - "Namespace declarations inside moved files are updated"
  artifacts:
    - path: "src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/UpInfo/GetSpaceInfoResponse.cs"
      provides: "Renamed Space→UpInfo response DTO"
      contains: "namespace Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.UpInfo"
    - path: "src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/AccountApi/CoinBalance.cs"
      provides: "IAccountApi-aligned coin balance DTO"
      contains: "namespace Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.AccountApi"
    - path: "src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/Nav/UserInfo.cs"
      provides: "INavApi-aligned user info DTO"
      contains: "namespace Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.Nav"
  key_links:
    - from: "Dtos/UpInfo/"
      to: "IApiApi #region UpInfo"
      via: "namespace alignment"
    - from: "Dtos/AccountApi/CoinBalance.cs"
      to: "IAccountApi"
      via: "namespace alignment"
    - from: "Dtos/Nav/UserInfo.cs"
      to: "INavApi"
      via: "namespace alignment"
---

<objective>
Move and rename three DTO locations so the folder structure mirrors the interface grouping from v4.0.0.4.

Purpose: After consolidating 8 api.bilibili.com interfaces into IApiApi, the DTO sub-folders are misaligned. This plan executes the physical file moves and updates namespace declarations inside the moved files.

Output: Three new folders (UpInfo/, AccountApi/, Nav/) with correct namespace declarations. Old locations deleted.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key interfaces this plan aligns DTOs with -->

IApiApi — [Headers("Host: api.bilibili.com")]
  #region UpInfo → GetSpaceInfo() returns GetSpaceInfoResponse; uses GetSpaceInfoDto

IAccountApi — [Headers("Host: account.bilibili.com")]
  GetCoinBalanceAsync() → Task<BiliApiResponse<CoinBalance>>

INavApi — [Headers("Host: api.bilibili.com")] (separate from IApiApi due to WBI circular dep)
  GetNavAsync() → Task<BiliApiResponse<UserInfo>>

Current DTO folder → new DTO folder mapping:
  Dtos/Space/   → Dtos/UpInfo/       (3 files: GetSpaceInfoFullDto, GetSpaceInfoResponse, UpInfo)
  Dtos/Coin/CoinBalance.cs → Dtos/AccountApi/CoinBalance.cs  (1 file; other Coin/ files stay)
  Dtos/UserInfo.cs (root) → Dtos/Nav/UserInfo.cs             (1 file; no other Nav/ files)

Current namespace → new namespace:
  Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.Space   → Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.UpInfo
  Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.Coin    → Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.AccountApi  (CoinBalance.cs only)
  Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos         → Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.Nav          (UserInfo.cs only)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Space/ → UpInfo/ (create folder, move 3 files, update namespaces, delete old folder)</name>
  <files>
    src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/UpInfo/GetSpaceInfoFullDto.cs
    src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/UpInfo/GetSpaceInfoResponse.cs
    src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/UpInfo/UpInfo.cs
  </files>
  <action>
    1. Create folder `Dtos/UpInfo/`
    2. Read each of the 3 files in `Dtos/Space/`:
       - GetSpaceInfoFullDto.cs
       - GetSpaceInfoResponse.cs
       - UpInfo.cs
    3. For each file: change `namespace Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.Space` → `namespace Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.UpInfo` and write to `Dtos/UpInfo/`
    4. Delete the 3 old files from `Dtos/Space/`
    5. Delete the now-empty `Dtos/Space/` folder

    Note: Do NOT rename any class names (e.g., `GetSpaceInfoResponse` stays as-is). Only the namespace declaration changes.
  </action>
  <verify>
    Test-Path "Dtos/UpInfo/GetSpaceInfoResponse.cs" → True
    Test-Path "Dtos/Space/" → False
    Select-String "Dtos.UpInfo" Dtos/UpInfo/GetSpaceInfoResponse.cs → match found
  </verify>
  <done>All 3 files exist in UpInfo/ with updated namespaces; Space/ folder deleted</done>
</task>

<task type="auto">
  <name>Task 2: Move CoinBalance.cs → AccountApi/ (create folder, move 1 file, update namespace)</name>
  <files>
    src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/AccountApi/CoinBalance.cs
  </files>
  <action>
    1. Create folder `Dtos/AccountApi/`
    2. Read `Dtos/Coin/CoinBalance.cs`
    3. Change namespace: `Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.Coin` → `Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.AccountApi`
    4. Write to `Dtos/AccountApi/CoinBalance.cs`
    5. Delete `Dtos/Coin/CoinBalance.cs`

    IMPORTANT: Do NOT delete or touch any other files in Dtos/Coin/ — AddCoinRequest.cs, DonatedCoinsForVideo.cs, GetAlreadyDonatedCoinsRequest.cs stay in Dtos/Coin/ and keep namespace `Dtos.Coin`.
  </action>
  <verify>
    Test-Path "Dtos/AccountApi/CoinBalance.cs" → True
    Test-Path "Dtos/Coin/CoinBalance.cs" → False
    Test-Path "Dtos/Coin/AddCoinRequest.cs" → True (unchanged)
    Select-String "Dtos.AccountApi" Dtos/AccountApi/CoinBalance.cs → match found
  </verify>
  <done>CoinBalance.cs in AccountApi/ with updated namespace; removed from Coin/; other Coin/ files untouched</done>
</task>

<task type="auto">
  <name>Task 3: Move root UserInfo.cs → Nav/ (create folder, move 1 file, update namespace)</name>
  <files>
    src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/Nav/UserInfo.cs
  </files>
  <action>
    1. Create folder `Dtos/Nav/`
    2. Read `Dtos/UserInfo.cs`
    3. Change namespace declaration: `namespace Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos` → `namespace Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.Nav`
    4. Write to `Dtos/Nav/UserInfo.cs`
    5. Delete `Dtos/UserInfo.cs`

    Note: VipType.cs, VipStatus.cs, VipPrivilegeType.cs at root level are NOT moved — only UserInfo.cs is relocated. These Vip* types remain in root `Dtos` namespace (they are used broadly and not interface-specific).
  </action>
  <verify>
    Test-Path "Dtos/Nav/UserInfo.cs" → True
    Test-Path "Dtos/UserInfo.cs" → False
    Select-String "Dtos.Nav" Dtos/Nav/UserInfo.cs → match found
  </verify>
  <done>UserInfo.cs in Nav/ with updated namespace; root-level copy deleted</done>
</task>

</tasks>

<verification>
After all 3 tasks:
- `Test-Path Dtos/UpInfo/` → True; `Test-Path Dtos/Space/` → False
- `Test-Path Dtos/AccountApi/CoinBalance.cs` → True; `Test-Path Dtos/Coin/CoinBalance.cs` → False
- `Test-Path Dtos/Nav/UserInfo.cs` → True; `Test-Path Dtos/UserInfo.cs` → False
- Build WILL fail at this point (consumers not yet updated — that is expected and handled in Plan 02)
</verification>

<success_criteria>
- All 5 destination files created with correct new namespaces
- All 5 source files/folders deleted
- Other files in Dtos/Coin/ unchanged
- VipType.cs, VipStatus.cs, VipPrivilegeType.cs at Dtos root unchanged
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system | Agent creates/deletes files on disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | File move | accept | File moves are reversible via git; no runtime security impact |
</threat_model>

<output>
After completion, create `.planning/phases/12-agent-dto-reorganization/12-01-SUMMARY.md`
</output>
