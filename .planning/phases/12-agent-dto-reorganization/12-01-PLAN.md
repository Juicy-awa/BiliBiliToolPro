---
phase: 12-agent-dto-reorganization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/ApiApi/UpInfo/GetSpaceInfoFullDto.cs
  - src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/ApiApi/UpInfo/GetSpaceInfoResponse.cs
  - src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/ApiApi/UpInfo/UpInfo.cs
autonomous: true
requirements: [DTO-01, DTO-02, DTO-03, DTO-04]

must_haves:
  truths:
    - "Dtos/ApiApi/UpInfo/ folder exists with 3 files (GetSpaceInfoFullDto, GetSpaceInfoResponse, UpInfo)"
    - "Top-level Dtos/UpInfo/ folder no longer exists"
    - "Namespace declarations inside moved files use Dtos.ApiApi.UpInfo"
    - "Dtos/AccountApi/ remains the root for IAccountApi-owned DTOs"
    - "Dtos/NavApi/ remains the root for the Nav DTO group"
  artifacts:
    - path: "src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/ApiApi/UpInfo/GetSpaceInfoResponse.cs"
      provides: "IApiApi-owned UpInfo response DTO under an interface-first Api root"
      contains: "namespace Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.ApiApi.UpInfo"
    - path: "src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/ApiApi/UpInfo/UpInfo.cs"
      provides: "UpInfo DTO under the IApiApi root"
      contains: "namespace Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.ApiApi.UpInfo"
  key_links:
    - from: "Dtos/ApiApi/UpInfo/"
      to: "IApiApi"
      via: "interface-first directory ownership"
    - from: "Dtos/ApiApi/UpInfo/"
      to: "IApiApi #region UpInfo"
      via: "nested sub-group under the Api interface root"
---

<objective>
Move the current top-level `Dtos/UpInfo/` slice under an interface-first `Dtos/ApiApi/` root and update its namespaces accordingly.

Purpose: The previous Phase 12 layout treated `UpInfo` as a top-level DTO root. The clarified rule is interface-first ownership: `IApiApi` gets an `Api/` root, with `UpInfo/` as a nested slice under it.

Output: `Dtos/ApiApi/UpInfo/` exists with correct namespaces; the old top-level `Dtos/UpInfo/` path is removed.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-agent-dto-reorganization/12-CONTEXT.md

<interfaces>
<!-- Key interfaces this plan aligns DTOs with -->

IApiApi — [Headers("Host: api.bilibili.com")]
  #region UpInfo → GetSpaceInfo() returns GetSpaceInfoResponse; uses GetSpaceInfoDto

Current DTO folder → new DTO folder mapping:
  Dtos/UpInfo/   → Dtos/ApiApi/UpInfo/   (3 files: GetSpaceInfoFullDto, GetSpaceInfoResponse, UpInfo)

Current namespace → new namespace:
  Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.UpInfo   → Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.ApiApi.UpInfo

Stable interface-owned roots (do not move in this plan):
  Dtos/AccountApi/  → IAccountApi
  Dtos/NavApi/         → INavApi / Nav DTO group
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move top-level UpInfo slice under Api/ root and update namespaces</name>
  <files>
    src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/ApiApi/UpInfo/GetSpaceInfoFullDto.cs
    src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/ApiApi/UpInfo/GetSpaceInfoResponse.cs
    src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/ApiApi/UpInfo/UpInfo.cs
  </files>
  <action>
    1. Create folder `Dtos/ApiApi/UpInfo/`.
    2. Read each of the 3 files currently under `Dtos/UpInfo/`:
       - GetSpaceInfoFullDto.cs
       - GetSpaceInfoResponse.cs
       - UpInfo.cs
    3. Move each file into `Dtos/ApiApi/UpInfo/` using a history-preserving move strategy per D-01.
    4. For each moved file, change `namespace Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.UpInfo` → `namespace Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.ApiApi.UpInfo`.
    5. Delete the now-empty top-level `Dtos/UpInfo/` folder.

    Keep all DTO class names unchanged. This plan changes directory ownership and namespaces only.
  </action>
  <verify>
    Test-Path "Dtos/ApiApi/UpInfo/GetSpaceInfoResponse.cs" → True
    Test-Path "Dtos/UpInfo/" → False
    Select-String "Dtos.ApiApi.UpInfo" Dtos/ApiApi/UpInfo/GetSpaceInfoResponse.cs → match found
  </verify>
  <done>All 3 files live under Dtos/ApiApi/UpInfo/ with updated namespaces; the top-level Dtos/UpInfo/ folder is removed</done>
</task>

</tasks>

<verification>
After the move:
- `Test-Path Dtos/ApiApi/UpInfo/` → True
- `Test-Path Dtos/UpInfo/` → False
- `Test-Path Dtos/AccountApi/CoinBalance.cs` → True (unchanged)
- `Test-Path Dtos/NavApi/UserInfo.cs` → True (unchanged)
- Build may fail until consumers are updated in Plan 02 — that is expected
</verification>

<success_criteria>
- All 3 UpInfo DTO files live under `Dtos/ApiApi/UpInfo/`
- Top-level `Dtos/UpInfo/` no longer exists
- Namespaces use `Dtos.ApiApi.UpInfo`
- `Dtos/AccountApi/` and `Dtos/NavApi/` remain the stable interface-owned roots
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system | Agent creates/deletes files on disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | File move | accept | File moves are reversible via git; no runtime security impact |
</threat_model>

<output>
After completion, create `.planning/phases/12-agent-dto-reorganization/12-01-SUMMARY.md`
</output>
