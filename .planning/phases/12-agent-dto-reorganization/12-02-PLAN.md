---
phase: 12-agent-dto-reorganization
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IApiApi.cs
  - src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IAccountApi.cs
  - src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/INavApi.cs
  - src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Services/WbiService.cs
  - src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/Relation/GetFollowingsResponse.cs
  - src/Ray.BiliBiliTool.DomainService/AccountDomainService.cs
  - src/Ray.BiliBiliTool.DomainService/LiveDomainService.cs
  - src/Ray.BiliBiliTool.DomainService/DonateCoinDomainService.cs
  - src/Ray.BiliBiliTool.DomainService/ChargeDomainService.cs
  - src/Ray.BiliBiliTool.DomainService/VipPrivilegeDomainService.cs
  - src/Ray.BiliBiliTool.DomainService/MangaDomainService.cs
  - src/Ray.BiliBiliTool.Application/VipPrivilegeTaskAppService.cs
  - src/Ray.BiliBiliTool.Application/VipBigPointAppService.cs
  - src/Ray.BiliBiliTool.Application/MangaPrivilegeTaskAppService.cs
  - src/Ray.BiliBiliTool.Application/DailyTaskAppService.cs
  - src/Ray.BiliBiliTool.Application/ChargeTaskAppService.cs
  - test/Ray.BiliBiliTool.Agent.FunctionalTests/LiveApiTest.cs
  - test/Ray.BiliBiliTool.Agent.FunctionalTests/AccountApiTests.cs
  - test/Ray.BiliBiliTool.CharacterizationTests/DailyTaskCharacterizationTests.cs
  - test/DomainServiceTest/CalculateUpgradeTimeTest.cs
autonomous: true
requirements: [DTO-05, DTO-06]

must_haves:
  truths:
    - "dotnet build succeeds with 0 errors"
    - "Architecture tests pass 4/4"
    - "Integration tests pass 7/7"
    - "No file references Dtos.Space namespace"
    - "IAccountApi uses Dtos.AccountApi namespace for CoinBalance"
    - "INavApi uses Dtos.Nav namespace for UserInfo"
  artifacts:
    - path: "src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IApiApi.cs"
      provides: "IApiApi with updated Space→UpInfo using directive"
      contains: "using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.UpInfo"
    - path: "src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IAccountApi.cs"
      provides: "IAccountApi with AccountApi namespace for CoinBalance"
      contains: "using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.AccountApi"
  key_links:
    - from: "IAccountApi.cs"
      to: "Dtos/AccountApi/CoinBalance.cs"
      via: "using directive"
    - from: "INavApi.cs"
      to: "Dtos/Nav/UserInfo.cs"
      via: "using directive"
    - from: "AccountDomainService.cs"
      to: "Dtos/Nav/UserInfo.cs"
      via: "using directive"
---

<objective>
Fix all consuming files so they reference the new DTO namespaces introduced in Plan 01. Verify build, architecture tests, and integration tests all pass.

Purpose: Plan 01 moved files and updated namespace declarations inside the DTO files. This plan updates all callers — interfaces, domain services, application services, and tests.

Output: 0 build errors; arch 4/4; integration 7/7. Phase complete.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-agent-dto-reorganization/12-01-SUMMARY.md

<interfaces>
<!-- Namespace changes from Plan 01 that consumers must adopt -->

Change A: Dtos.Space → Dtos.UpInfo
  Affected files with "using Dtos.Space":
    - IApiApi.cs
    - DomainService/LiveDomainService.cs
    - DomainService/AccountDomainService.cs
    - DomainService/DonateCoinDomainService.cs
    - Dtos/Relation/GetFollowingsResponse.cs          ← internal DTO cross-reference
    - FunctionalTests/LiveApiTest.cs

Change B: Dtos.Coin (for CoinBalance only) → Dtos.AccountApi
  Files that use CoinBalance and import Dtos.Coin:
    - IAccountApi.cs       → remove "using Dtos.Coin", add "using Dtos.AccountApi"
    - FunctionalTests/AccountApiTests.cs → same swap
  Files that use Dtos.Coin but NOT CoinBalance (keep using Dtos.Coin):
    - IApiApi.cs           → keep "using Dtos.Coin" (AddCoinRequest, DonatedCoinsForVideo, GetAlreadyDonatedCoinsRequest)
    - DonateCoinDomainService.cs → keep "using Dtos.Coin"
    - BiliAgentTest/VideoApiTest.cs → keep "using Dtos.Coin"

Change C: root Dtos (UserInfo) → Dtos.Nav
  Files that reference UserInfo class directly — each needs "using Dtos.Nav" added:
    - INavApi.cs                         (already has "using Dtos" — add "using Dtos.Nav")
    - WbiService.cs                      (already has "using Dtos" — add "using Dtos.Nav")
    - DomainService/AccountDomainService.cs  (already has "using Dtos.Space" — add "using Dtos.Nav")
    - DomainService/ChargeDomainService.cs   (check for "using Dtos" — add "using Dtos.Nav")
    - DomainService/VipPrivilegeDomainService.cs  (check — add "using Dtos.Nav")
    - DomainService/MangaDomainService.cs    (check — add "using Dtos.Nav")
    - Application/VipPrivilegeTaskAppService.cs   (already has "using Dtos" — add "using Dtos.Nav")
    - Application/VipBigPointAppService.cs        (already has "using Dtos" — add "using Dtos.Nav")
    - Application/MangaPrivilegeTaskAppService.cs (already has "using Dtos" — add "using Dtos.Nav")
    - Application/DailyTaskAppService.cs          (check if uses UserInfo — add if so)
    - Application/ChargeTaskAppService.cs         (check if uses UserInfo — add if so)
    - CharacterizationTests/DailyTaskCharacterizationTests.cs (already has "using Dtos" — add "using Dtos.Nav")
    - DomainServiceTest/CalculateUpgradeTimeTest.cs (already has "using Dtos" — add "using Dtos.Nav")
  Note: "using Dtos" must be KEPT in files that still use BiliApiResponse, BaseAppRequest, VipType, VipStatus, VipPrivilegeType from the root namespace.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Change A — replace "using Dtos.Space" with "using Dtos.UpInfo" in all 6 consumers</name>
  <files>
    src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IApiApi.cs
    src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/Relation/GetFollowingsResponse.cs
    src/Ray.BiliBiliTool.DomainService/LiveDomainService.cs
    src/Ray.BiliBiliTool.DomainService/AccountDomainService.cs
    src/Ray.BiliBiliTool.DomainService/DonateCoinDomainService.cs
    test/Ray.BiliBiliTool.Agent.FunctionalTests/LiveApiTest.cs
  </files>
  <action>
    In each file, replace the exact line:
      `using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.Space;`
    with:
      `using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.UpInfo;`

    No other changes to these files.
  </action>
  <verify>
    Select-String "Dtos.Space" src/ test/ -Recurse → 0 matches
    Select-String "Dtos.UpInfo" src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IApiApi.cs → match found
  </verify>
  <done>Zero references to Dtos.Space remain in the solution</done>
</task>

<task type="auto">
  <name>Task 2: Fix Change B — swap CoinBalance consumers from Dtos.Coin to Dtos.AccountApi</name>
  <files>
    src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IAccountApi.cs
    test/Ray.BiliBiliTool.Agent.FunctionalTests/AccountApiTests.cs
  </files>
  <action>
    In IAccountApi.cs:
      Replace `using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.Coin;`
      with    `using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.AccountApi;`

    In AccountApiTests.cs:
      Replace `using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.Coin;`
      with    `using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.AccountApi;`

    DO NOT touch IApiApi.cs, DonateCoinDomainService.cs, or BiliAgentTest/VideoApiTest.cs —
    those files use Dtos.Coin for AddCoinRequest / DonatedCoinsForVideo / GetAlreadyDonatedCoinsRequest
    which remain in Dtos/Coin/.
  </action>
  <verify>
    Select-String "Dtos.AccountApi" src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IAccountApi.cs → match found
    Select-String "Dtos.AccountApi" test/Ray.BiliBiliTool.Agent.FunctionalTests/AccountApiTests.cs → match found
  </verify>
  <done>IAccountApi and AccountApiTests reference Dtos.AccountApi for CoinBalance</done>
</task>

<task type="auto">
  <name>Task 3: Fix Change C — add "using Dtos.Nav" to all UserInfo consumers; verify build + tests</name>
  <files>
    src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/INavApi.cs
    src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Services/WbiService.cs
    src/Ray.BiliBiliTool.DomainService/AccountDomainService.cs
    src/Ray.BiliBiliTool.DomainService/ChargeDomainService.cs
    src/Ray.BiliBiliTool.DomainService/VipPrivilegeDomainService.cs
    src/Ray.BiliBiliTool.DomainService/MangaDomainService.cs
    src/Ray.BiliBiliTool.Application/VipPrivilegeTaskAppService.cs
    src/Ray.BiliBiliTool.Application/VipBigPointAppService.cs
    src/Ray.BiliBiliTool.Application/MangaPrivilegeTaskAppService.cs
    src/Ray.BiliBiliTool.Application/DailyTaskAppService.cs
    src/Ray.BiliBiliTool.Application/ChargeTaskAppService.cs
    test/Ray.BiliBiliTool.CharacterizationTests/DailyTaskCharacterizationTests.cs
    test/DomainServiceTest/CalculateUpgradeTimeTest.cs
  </files>
  <action>
    Step 1 — For each file that references UserInfo class, ADD the line:
      `using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.Nav;`
    immediately after the last existing `using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos*` line.

    Before adding, check each file for actual UserInfo usage with grep/read. Skip files that don't use UserInfo.
    Keep all existing `using` directives — this is an ADDITION only, not a replacement.
    The root `using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos;` must be KEPT in files that also use
    BiliApiResponse, VipType, VipStatus, VipPrivilegeType, BiliPageResult, or BaseAppRequest.

    Step 2 — Run build:
      cd {solution root}
      dotnet build Ray.BiliBiliTool.sln --no-restore -v quiet 2>&1 | Select-String "error|warning"

    If build errors remain (unexpected files using UserInfo):
      Read the error, identify the file, add `using Dtos.Nav;` there too, rebuild.

    Step 3 — Run architecture tests:
      dotnet test test/Ray.BiliBiliTool.Test.Architecture/ --no-build -v quiet 2>&1 | tail -5

    Step 4 — Run integration tests:
      dotnet test test/Ray.BiliBiliTool.Test.Integration/ --no-build -v quiet 2>&1 | tail -5
  </action>
  <verify>
    <automated>dotnet build Ray.BiliBiliTool.sln -v quiet 2>&1 | Select-String "^Build succeeded"</automated>
  </verify>
  <done>
    - Build: 0 errors
    - dotnet test Architecture → 4/4 passed
    - dotnet test Integration → 7/7 passed
  </done>
</task>

</tasks>

<verification>
Final checks:
1. `Select-String "Dtos.Space" src/ test/ -Recurse -Include "*.cs"` → 0 matches
2. `dotnet build Ray.BiliBiliTool.sln` → "Build succeeded" with 0 errors
3. Architecture tests 4/4
4. Integration tests 7/7
</verification>

<success_criteria>
- Zero references to `Dtos.Space` in solution
- IAccountApi and AccountApiTests use `Dtos.AccountApi` for CoinBalance
- All UserInfo consumers have `using Dtos.Nav`
- Build 0 errors, Arch 4/4, Integration 7/7
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline | Compiler validates all namespace references |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering | using directives | accept | Compiler enforces correctness; no security boundary crossed |
</threat_model>

<output>
After completion, create `.planning/phases/12-agent-dto-reorganization/12-02-SUMMARY.md`
</output>
