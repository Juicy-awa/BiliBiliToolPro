---
phase: 12-agent-dto-reorganization
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IApiApi.cs
  - src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/Relation/GetFollowingsResponse.cs
  - src/Ray.BiliBiliTool.DomainService/AccountDomainService.cs
  - src/Ray.BiliBiliTool.DomainService/LiveDomainService.cs
  - src/Ray.BiliBiliTool.DomainService/DonateCoinDomainService.cs
  - test/Ray.BiliBiliTool.Agent.FunctionalTests/LiveApiTest.cs
autonomous: true
requirements: [DTO-05, DTO-06]

must_haves:
  truths:
    - "dotnet build succeeds with 0 errors"
    - "Architecture tests pass 4/4"
    - "Integration tests pass 7/7"
    - "No source or test file imports the old top-level Dtos.UpInfo namespace"
    - "IApiApi and all known consumers use Dtos.ApiApi.UpInfo or an alias rooted in Dtos.ApiApi.UpInfo"
    - "Compiler- or test-exposed extra consumers are fixed even if they were not listed in the initial plan"
  artifacts:
    - path: "src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IApiApi.cs"
      provides: "IApiApi with interface-first ApiApi/UpInfo references"
      contains: "using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.ApiApi.UpInfo"
    - path: "src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/Relation/GetFollowingsResponse.cs"
      provides: "Cross-DTO alias rooted in Dtos.ApiApi.UpInfo"
      contains: "Dtos.ApiApi.UpInfo.UpInfo"
  key_links:
    - from: "IAccountApi.cs"
      to: "Dtos/AccountApi/CoinBalance.cs"
      via: "remains unchanged while Api-rooted consumers are updated"
    - from: "IApiApi.cs"
      to: "Dtos/ApiApi/UpInfo/"
      via: "using directive and UpInfoDto alias"
    - from: "AccountDomainService.cs"
      to: "Dtos/ApiApi/UpInfo/"
      via: "using directive and UpInfoDto alias"
---

<objective>
Fix all consuming files so they reference the interface-first `Dtos.ApiApi.UpInfo` namespace introduced in Plan 01. Verify build, architecture tests, and integration tests all pass.

Purpose: Plan 01 rehomes the current UpInfo slice under the `Api/` root. This plan updates all callers, and because D-05 explicitly allows compiler-driven discovery, it also catches any additional consumers exposed by build or test failures.

Output: 0 build errors; arch 4/4; integration 7/7. Phase complete.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-agent-dto-reorganization/12-CONTEXT.md
@.planning/phases/12-agent-dto-reorganization/12-01-SUMMARY.md

<interfaces>
<!-- Namespace changes from Plan 01 that consumers must adopt -->

Change A: Dtos.UpInfo → Dtos.ApiApi.UpInfo
  Known files with "using Dtos.UpInfo" or aliases rooted in it:
    - IApiApi.cs
    - Dtos/Relation/GetFollowingsResponse.cs          ← internal DTO cross-reference
    - DomainService/LiveDomainService.cs
    - DomainService/AccountDomainService.cs
    - DomainService/DonateCoinDomainService.cs
    - FunctionalTests/LiveApiTest.cs

  Expected replacements:
    - `using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.UpInfo;`
      → `using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.ApiApi.UpInfo;`
    - `using UpInfoDto = Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.UpInfo.UpInfo;`
      → `using UpInfoDto = Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.ApiApi.UpInfo.UpInfo;`

Stable namespace roots that should remain unchanged:
  - `Dtos.AccountApi` for CoinBalance
  - `Dtos.NavApi` for the Nav DTO group
  - `Dtos.Coin` for non-CoinBalance coin request/response DTOs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all known UpInfo consumers from Dtos.UpInfo to Dtos.ApiApi.UpInfo</name>
  <files>
    src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IApiApi.cs
    src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/Relation/GetFollowingsResponse.cs
    src/Ray.BiliBiliTool.DomainService/AccountDomainService.cs
    src/Ray.BiliBiliTool.DomainService/LiveDomainService.cs
    src/Ray.BiliBiliTool.DomainService/DonateCoinDomainService.cs
    test/Ray.BiliBiliTool.Agent.FunctionalTests/LiveApiTest.cs
  </files>
  <action>
    Update each known `UpInfo` consumer to the new interface-first namespace root.

    Required replacements:
    - `using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.UpInfo;`
      → `using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.ApiApi.UpInfo;`
    - `using UpInfoDto = Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.UpInfo.UpInfo;`
      → `using UpInfoDto = Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.ApiApi.UpInfo.UpInfo;`

    Keep `Dtos.AccountApi`, `Dtos.NavApi`, and `Dtos.Coin` references unchanged. This task is only about the new `ApiApi/UpInfo` root.
  </action>
  <verify>
    Select-String "Dtos.UpInfo" src/ test/ -Recurse -Include "*.cs" → 0 matches or only files intentionally left for follow-up
    Select-String "Dtos.ApiApi.UpInfo" src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IApiApi.cs → match found
  </verify>
  <done>All known source/test consumers point to Dtos.ApiApi.UpInfo instead of the old top-level Dtos.UpInfo namespace</done>
</task>

<task type="auto">
  <name>Task 2: Run build and required test suites, then fix any additional compiler-exposed UpInfo consumers</name>
  <files>
    src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IApiApi.cs
    src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Dtos/Relation/GetFollowingsResponse.cs
    src/Ray.BiliBiliTool.DomainService/AccountDomainService.cs
    src/Ray.BiliBiliTool.DomainService/LiveDomainService.cs
    src/Ray.BiliBiliTool.DomainService/DonateCoinDomainService.cs
    test/Ray.BiliBiliTool.Agent.FunctionalTests/LiveApiTest.cs
  </files>
  <action>
    Step 1 — Build the solution:
      `dotnet build Ray.BiliBiliTool.sln --no-restore -v minimal`

    Step 2 — If the compiler reports additional `Dtos.UpInfo` or `Dtos.UpInfo.UpInfo` references outside the initial list, update those files too. Per D-05, the compiler result is authoritative and the initial file list is not a hard cap.

    Step 3 — Run architecture tests:
      `dotnet test test/Ray.BiliBiliTool.ArchitectureTests/ --no-build -v quiet`

    Step 4 — Run host integration tests:
      `dotnet test test/Ray.BiliBiliTool.Host.IntegrationTests/ --no-build -v quiet`
  </action>
  <verify>
    <automated>dotnet build Ray.BiliBiliTool.sln --no-restore -v minimal</automated>
  </verify>
  <done>
    - Build: 0 errors
    - Architecture tests: 4/4 passed
    - Host integration tests: 7/7 passed
    - No remaining source/test imports target the old top-level Dtos.UpInfo namespace
  </done>
</task>

</tasks>

<verification>
Final checks:
1. `Select-String "Dtos.UpInfo" src/ test/ -Recurse -Include "*.cs"` → 0 matches
2. `dotnet build Ray.BiliBiliTool.sln` → "Build succeeded" with 0 errors
3. Architecture tests 4/4
4. Host integration tests 7/7
</verification>

<success_criteria>
- Zero source/test references to the old top-level `Dtos.UpInfo` namespace
- Known consumers use `Dtos.ApiApi.UpInfo` or aliases rooted in `Dtos.ApiApi.UpInfo.UpInfo`
- Build 0 errors, Arch 4/4, Host Integration 7/7
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline | Compiler validates all namespace references |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering | using directives | accept | Compiler enforces correctness; no security boundary crossed |
</threat_model>

<output>
After completion, create `.planning/phases/12-agent-dto-reorganization/12-02-SUMMARY.md`
</output>
