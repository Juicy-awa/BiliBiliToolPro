---
phase: 02-host-safety-nets
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/Ray.BiliBiliTool.Application/Diagnostics/TaskFlowDiagnosticScope.cs
  - src/Ray.BiliBiliTool.Application/LoginTaskAppService.cs
  - src/Ray.BiliBiliTool.Application/DailyTaskAppService.cs
  - test/Ray.BiliBiliTool.CharacterizationTests/LoginTaskCharacterizationTests.cs
  - test/Ray.BiliBiliTool.CharacterizationTests/DailyTaskCharacterizationTests.cs
autonomous: true
requirements:
  - TEST-01
  - QUAL-02
must_haves:
  truths:
    - "Maintainers can run focused characterization tests that freeze the current observable Login orchestration behavior."
    - "Maintainers can run focused characterization tests that freeze the current observable DailyTask orchestration behavior, including the multi-account wrapper."
    - "Critical Login and DailyTask paths emit explicit diagnostic markers that later refactors can compare against without manual tracing."
  artifacts:
    - path: src/Ray.BiliBiliTool.Application/Diagnostics/TaskFlowDiagnosticScope.cs
      provides: Shared diagnostic marker helper for critical application task entry boundaries
      contains: structured flow marker or logging scope support
    - path: test/Ray.BiliBiliTool.CharacterizationTests/LoginTaskCharacterizationTests.cs
      provides: Login baseline characterization tests
      contains: current orchestration and diagnostic assertions
    - path: test/Ray.BiliBiliTool.CharacterizationTests/DailyTaskCharacterizationTests.cs
      provides: DailyTask baseline characterization tests
      contains: multi-account wrapper behavior and diagnostic assertions
  key_links:
    - from: test/Ray.BiliBiliTool.CharacterizationTests/LoginTaskCharacterizationTests.cs
      to: src/Ray.BiliBiliTool.Application/LoginTaskAppService.cs
      via: app-service boundary characterization
      pattern: DoTaskAsync
    - from: test/Ray.BiliBiliTool.CharacterizationTests/DailyTaskCharacterizationTests.cs
      to: src/Ray.BiliBiliTool.Application/DailyTaskAppService.cs
      via: app-service boundary characterization
      pattern: DoTaskAsync
    - from: src/Ray.BiliBiliTool.Application/Diagnostics/TaskFlowDiagnosticScope.cs
      to: src/Ray.BiliBiliTool.Application/Attributes/TaskInterceptorAttribute.cs
      via: complementary diagnostic markers
      pattern: task|flow
---

<objective>
Freeze the current Login and DailyTask behavior at their application-entry boundaries and make their diagnostics explicit enough for future comparisons.

Purpose: Give the later Login and DailyTask refactor phases a trustworthy baseline.
Output: Characterization tests for both critical flows plus explicit diagnostic markers around their current execution paths.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/01-boundary-guardrails/01-boundary-guardrails-01-SUMMARY.md
@.planning/phases/01-boundary-guardrails/01-boundary-guardrails-02-SUMMARY.md
@.planning/phases/02-host-safety-nets/02-RESEARCH.md
@src/Ray.BiliBiliTool.Application/LoginTaskAppService.cs
@src/Ray.BiliBiliTool.Application/DailyTaskAppService.cs
@src/Ray.BiliBiliTool.Application/BaseMultiAccountsAppService.cs
@src/Ray.BiliBiliTool.Application/Attributes/TaskInterceptorAttribute.cs

<interfaces>
From src/Ray.BiliBiliTool.Application.Contracts/ILoginTaskAppService.cs:
```csharp
public interface ILoginTaskAppService : IAppService;
```

From src/Ray.BiliBiliTool.Application.Contracts/IDailyTaskAppService.cs:
```csharp
public interface IDailyTaskAppService : IAppService;
```

From src/Ray.BiliBiliTool.Application/BaseMultiAccountsAppService.cs:
```csharp
public override async Task DoTaskAsync(CancellationToken cancellationToken = default)
protected abstract Task DoTaskAccountAsync(BiliCookie ck, CancellationToken cancellationToken = default);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit diagnostic markers for Login and DailyTask application-entry execution</name>
  <files>src/Ray.BiliBiliTool.Application/Diagnostics/TaskFlowDiagnosticScope.cs, src/Ray.BiliBiliTool.Application/LoginTaskAppService.cs, src/Ray.BiliBiliTool.Application/DailyTaskAppService.cs</files>
  <action>Add a minimal diagnostic helper or logging-scope mechanism that marks Login and DailyTask flow execution at the application-entry boundary. Reuse the existing `TaskInterceptorAttribute`/logging model instead of introducing a tracing framework. The goal is to make future comparisons possible with stable, explicit markers while keeping runtime behavior unchanged.</action>
  <verify>
    <automated>rg "Diagnostic|Flow|LoginTask|DailyTask" src/Ray.BiliBiliTool.Application</automated>
  </verify>
  <done>Login and DailyTask application-entry execution paths emit deliberate, testable diagnostic markers in addition to their existing task-boundary logs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze Login and DailyTask orchestration with characterization tests</name>
  <files>test/Ray.BiliBiliTool.CharacterizationTests/LoginTaskCharacterizationTests.cs, test/Ray.BiliBiliTool.CharacterizationTests/DailyTaskCharacterizationTests.cs</files>
  <behavior>
    - Test 1: Login flow preserves its current observable step order from QR retrieval through cookie persistence, and emits the expected task and diagnostic markers.
    - Test 2: DailyTask flow preserves its current gated behavior and task sequence for an enabled account execution path.
    - Test 3: DailyTask multi-account entry keeps its current wrapper behavior and continues after per-account exceptions in the current way.
  </behavior>
  <action>Implement characterization tests against the application-entry seams for `LoginTaskAppService` and `DailyTaskAppService`. Use controlled doubles or narrow seams to freeze call ordering, skip/gate behavior, and emitted diagnostics without hitting real remote APIs. Keep the tests focused on current observable behavior, not on cleaning up the implementation yet.</action>
  <verify>
    <automated>dotnet test test/Ray.BiliBiliTool.CharacterizationTests/Ray.BiliBiliTool.CharacterizationTests.csproj</automated>
  </verify>
  <done>Characterization tests freeze the current Login and DailyTask behavior and assert diagnostic output strongly enough to support later refactor comparisons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app-service entry -> diagnostics | Baseline comparison breaks if diagnostic markers are ambiguous or inconsistent |
| characterization tests -> current orchestration behavior | Over-mocked tests can stop reflecting the real current behavior and give false confidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | R | Login and DailyTask diagnostics | mitigate | Add explicit application-entry markers and assert them in characterization tests so later comparisons are reproducible. |
| T-02-05 | T | characterization baseline | mitigate | Freeze observable sequence and gating behavior at the app-service boundary, not private helper implementation details. |
| T-02-06 | D | DailyTask multi-account path | mitigate | Include multi-account wrapper behavior in the baseline so later refactors cannot accidentally narrow or skip current execution semantics. |
</threat_model>

<verification>
- `dotnet test test/Ray.BiliBiliTool.CharacterizationTests/Ray.BiliBiliTool.CharacterizationTests.csproj`
</verification>

<success_criteria>
- Login and DailyTask have dedicated characterization tests at their current application-entry boundaries.
- Explicit diagnostic markers exist and are asserted by the tests.
- Future refactor work can compare current and changed behavior without manual log spelunking.
</success_criteria>

<output>
After completion, create `.planning/phases/02-host-safety-nets/02-host-safety-nets-02-SUMMARY.md`
</output>