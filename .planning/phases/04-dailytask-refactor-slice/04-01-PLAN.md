---
phase: 04-dailytask-refactor-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Ray.BiliBiliTool.Application/DailyTaskAppService.cs
autonomous: true
requirements: [FLOW-02]

must_haves:
  truths:
    - "DailyTaskAppService internal structure is clarified with comprehensive XML documentation"
    - "Six-step workflow (SetCookies → Login → GetStatus → WatchAndShare → AddCoins → ReceiveVipPrivilege) remains intact with documented purpose"
    - "Multi-account error handling behavior is preserved exactly as frozen by characterization tests"
    - "TaskInterceptor attributes and diagnostic scope markers remain in place"
    - "All characterization tests from Phase 2 continue to pass without modification"
  artifacts:
    - path: "src/Ray.BiliBiliTool.Application/DailyTaskAppService.cs"
      provides: "Refactored DailyTaskAppService with comprehensive XML documentation"
      min_lines: 170
  key_links:
    - from: "src/Ray.BiliBiliTool.Application/DailyTaskAppService.cs"
      to: "test/Ray.BiliBiliTool.CharacterizationTests/DailyTaskCharacterizationTests.cs"
      via: "validation of frozen behavior"
      pattern: "Should\\.Be\\(|Should\\.Contain\\("
---

<objective>
Clarify DailyTaskAppService internal structure with comprehensive XML documentation following the Phase 3 Login refactor pattern while preserving all observable behavior frozen by characterization tests.

Purpose: Make the six-step DailyTask workflow clearer for maintainers without changing any functional behavior that might break automation reliability.

Output: Refactored DailyTaskAppService.cs with class-level and method-level XML documentation explaining orchestration, plus inline comments clarifying configuration-driven branching and platform-aware persistence.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-dailytask-refactor-slice/04-CONTEXT.md
@.planning/phases/03-login-refactor-slice/03-01-SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md

<!-- Phase 3 established the documentation pattern for application service refactors -->
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor DailyTaskAppService internal structure for clarity</name>
  <files>src/Ray.BiliBiliTool.Application/DailyTaskAppService.cs</files>
  
  <read_first>
    <!-- Current implementation to understand before refactoring -->
    - src/Ray.BiliBiliTool.Application/DailyTaskAppService.cs
    
    <!-- Phase 3 pattern to replicate -->
    - src/Ray.BiliBiliTool.Application/LoginTaskAppService.cs
    
    <!-- Test expectations that constrain refactor -->
    - test/Ray.BiliBiliTool.CharacterizationTests/DailyTaskCharacterizationTests.cs
    
    <!-- Architecture boundaries to respect -->
    - .planning/codebase/ARCHITECTURE.md
    - .planning/codebase/CONVENTIONS.md
  </read_first>
  
  <action>
Add comprehensive XML documentation to DailyTaskAppService following the Phase 3 LoginTaskAppService pattern established in commit c6e04fd.

**Class-level XML documentation:**
Add a `<summary>` block explaining the overall six-step DailyTask workflow orchestration:
1. Cookie setup and validation (SetCookiesAsync)
2. User authentication (Login)  
3. Task status retrieval (GetDailyTaskStatus)
4. Watch and share video completion (WatchAndShareVideo)
5. Coin donation to videos or articles (AddCoins)
6. VIP privilege claiming (ReceiveVipPrivilege)

Explain the multi-account wrapper behavior (BaseMultiAccountsAppService inheritance) and configuration-driven flow control.

**Method-level XML documentation:**
Add `<summary>` blocks to all methods (public and private):
- DoTaskAccountAsync: Explain TaskFlowDiagnosticScope wrapping and the six-step sequence
- SetCookiesAsync: Explain cookie completeness check and platform-aware persistence
- Login: Explain authentication and exp logging
- GetDailyTaskStatus: Explain task completion status retrieval
- WatchAndShareVideo: Explain conditional execution based on IsWatchVideo/IsShareVideo flags
- AddCoins: Explain LV6 optimization and article-first vs video-first donation routing
- ReceiveVipPrivilege: Explain monthly privilege claiming and post-claim account refresh
- SaveCookieAsync: Explain QingLong vs JSON file persistence routing

**Inline comments:**
Add English inline comments to clarify:
- Platform routing logic in SaveCookieAsync (QingLong environment variable updates vs JSON file writes)
- Configuration-driven branching points (IsEnable early-return, SaveCoinsWhenLv6 optimization, IsDonateCoinForArticle routing)
- Multi-step conditional flows (article coin donation fallback to video donation)

**DO NOT modify:**
- Any Chinese comments (扫码登录, set cookie, 持久化cookie, etc.) — these are validated by characterization tests
- TaskInterceptor attribute parameters or placement
- Method signatures, return types, or parameter lists
- Control flow logic, conditional branches, or loop structures
- Domain service method calls or their parameters
- Exception handling patterns or error logging
- Any code that affects observable behavior frozen by DailyTaskCharacterizationTests

**Preserve exactly:**
- All TaskInterceptor attributes with their exact string labels ("每日任务", "登录", "观看、分享视频", "投币", "领取大会员福利")
- TaskFlowDiagnosticScope.ExecuteAsync wrapping with "DailyTask" label
- Multi-account error resilience (BaseMultiAccountsAppService base class behavior)
- Configuration option reads (_dailyTaskOptions.IsEnable, IsWatchVideo, IsShareVideo, SaveCoinsWhenLv6, IsDonateCoinForArticle)
- Platform detection via configuration.GetSection("PlatformType")

This is a documentation-only refactor. The code's observable behavior must be 100% identical before and after.
  </action>
  
  <verify>
    <automated>
cd d:\Codes\My\Bili\branch\blazor\BiliBiliToolPro
dotnet build src/Ray.BiliBiliTool.Application/Ray.BiliBiliTool.Application.csproj --no-restore
if ($LASTEXITCODE -ne 0) { exit 1 }

# Verify XML documentation was added
$content = Get-Content src/Ray.BiliBiliTool.Application/DailyTaskAppService.cs -Raw
if (-not ($content -match '/// <summary>[\s\S]*?六步[\s\S]*?DailyTask')) {
    Write-Error "Class-level XML documentation missing or incomplete"
    exit 1
}
if (-not ($content -match 'DoTaskAccountAsync[\s\S]{0,200}/// <summary>')) {
    Write-Error "DoTaskAccountAsync XML documentation missing"
    exit 1
}
if (-not ($content -match 'SetCookiesAsync[\s\S]{0,200}/// <summary>')) {
    Write-Error "SetCookiesAsync XML documentation missing"
    exit 1
}
if (-not ($content -match 'AddCoins[\s\S]{0,200}/// <summary>')) {
    Write-Error "AddCoins XML documentation missing"
    exit 1
}
Write-Host "✓ XML documentation verified" -ForegroundColor Green
    </automated>
  </verify>
  
  <acceptance_criteria>
- `src/Ray.BiliBiliTool.Application/DailyTaskAppService.cs` contains class-level XML summary documenting the six-step workflow
- DoTaskAccountAsync method has XML documentation explaining TaskFlowDiagnosticScope and the sequence
- All six workflow methods (SetCookiesAsync, Login, GetDailyTaskStatus, WatchAndShareVideo, AddCoins, ReceiveVipPrivilege) have XML summaries
- SaveCookieAsync method has XML documentation explaining platform routing
- Inline English comments clarify configuration-driven branching (IsEnable, SaveCoinsWhenLv6, IsDonateCoinForArticle, platform detection)
- All Chinese comments remain unchanged (扫码登录, set cookie, 持久化cookie, etc.)
- All TaskInterceptor attributes remain in exact original positions with original labels
- TaskFlowDiagnosticScope.ExecuteAsync wrapping preserved with "DailyTask" label
- BaseMultiAccountsAppService inheritance unchanged
- Build succeeds with no warnings in DailyTaskAppService.cs
  </acceptance_criteria>
  
  <done>
DailyTaskAppService has comprehensive XML documentation explaining the six-step workflow orchestration, all methods are documented, inline comments clarify configuration-driven behavior, and all TaskInterceptor attributes and diagnostic markers are preserved exactly.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Validate refactor preserves all frozen behavior</name>
  <files>test/Ray.BiliBiliTool.CharacterizationTests/DailyTaskCharacterizationTests.cs</files>
  
  <read_first>
    - test/Ray.BiliBiliTool.CharacterizationTests/DailyTaskCharacterizationTests.cs
    - test/Ray.BiliBiliTool.Host.IntegrationTests/WebStartupIntegrationTests.cs
    - test/Ray.BiliBiliTool.ArchitectureTests/
  </read_first>
  
  <action>
Run the full test suite to validate that the DailyTaskAppService refactor preserves all observable behavior frozen by Phase 2.

**Validation sequence:**
1. **Characterization tests** — Must pass without modification (DailyTaskCharacterizationTests.cs):
   - Daily_task_enabled_path_preserves_current_sequence_and_markers
   - Daily_task_multi_account_wrapper_continues_after_account_failure
   
   These tests freeze:
   - Six-step call sequence (SetCookieAsync → SaveCookieToJsonFileAsync → LoginByCookie#1 → GetDailyTaskStatus → WatchAndShareVideo → AddCoinsForVideos → ReceiveVipPrivilege)
   - Diagnostic markers (FlowStart DailyTask, FlowCompleted DailyTask, FlowFailed DailyTask)
   - TaskInterceptor logging ("每日任务", "登录", "观看、分享视频", "投币", "领取大会员福利")
   - Multi-account continue-after-failure behavior (LoginByCookie called twice even when first fails)

2. **Host integration tests** — Must continue passing (WebStartupIntegrationTests.cs)

3. **Architecture tests** — Must continue passing (dependency direction guardrails from Phase 1)

**Expected outcome:**
All tests pass on first run. If ANY test fails, the refactor violated frozen behavior and must be corrected before proceeding.

**DO NOT:**
- Modify characterization test expectations to match refactored code
- Skip or comment out failing tests
- Proceed to completion if tests fail

**Acceptance:** Zero test failures, zero test skips, all assertions green.
  </action>
  
  <verify>
    <automated>
cd d:\Codes\My\Bili\branch\blazor\BiliBiliToolPro

# Run characterization tests specifically for DailyTask
dotnet test test/Ray.BiliBiliTool.CharacterizationTests/Ray.BiliBiliTool.CharacterizationTests.csproj --filter "FullyQualifiedName~DailyTaskCharacterizationTests" --no-build --verbosity normal
if ($LASTEXITCODE -ne 0) { 
    Write-Error "Characterization tests FAILED - refactor violated frozen behavior"
    exit 1
}
Write-Host "✓ Characterization tests passed" -ForegroundColor Green

# Run host integration tests
dotnet test test/Ray.BiliBiliTool.Host.IntegrationTests/Ray.BiliBiliTool.Host.IntegrationTests.csproj --no-build --verbosity normal
if ($LASTEXITCODE -ne 0) { 
    Write-Error "Host integration tests FAILED"
    exit 1
}
Write-Host "✓ Host integration tests passed" -ForegroundColor Green

# Run architecture tests
dotnet test test/Ray.BiliBiliTool.ArchitectureTests/Ray.BiliBiliTool.ArchitectureTests.csproj --no-build --verbosity normal
if ($LASTEXITCODE -ne 0) { 
    Write-Error "Architecture tests FAILED"
    exit 1
}
Write-Host "✓ Architecture tests passed" -ForegroundColor Green

Write-Host "`n✓ All validation passed - refactor preserves frozen behavior" -ForegroundColor Green
    </automated>
  </verify>
  
  <acceptance_criteria>
- All DailyTaskCharacterizationTests pass without modification
- Host integration tests (WebStartupIntegrationTests) pass
- Architecture tests pass
- No test failures, no test skips
- Test output confirms six-step sequence preservation
- Test output confirms diagnostic marker presence (FlowStart, FlowCompleted, FlowFailed)
- Test output confirms TaskInterceptor logging (每日任务, 登录, etc.)
- Test output confirms multi-account error resilience (continues after first account failure)
  </acceptance_criteria>
  
  <done>
All characterization tests, host integration tests, and architecture tests pass. The DailyTaskAppService refactor is validated to preserve 100% of observable behavior frozen in Phase 2.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application → Domain | DailyTaskAppService orchestrates domain services; no untrusted input crosses here (cookies already validated) |
| External → Application | Out of scope — Phase 4 focuses on internal refactor, not integration boundaries (deferred to Phase 6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | DailyTaskAppService.SaveCookieAsync | accept | Cookie persistence routing (QingLong vs JSON) already established in existing code; no new information disclosure risk introduced by documentation-only refactor |
| T-04-02 | Denial of Service | DailyTaskAppService.DoTaskAccountAsync | accept | Multi-account error handling preserves existing resilience; documentation clarifies but does not weaken error boundaries |

**Note:** Phase 4 is a documentation-only refactor with zero behavior changes. No new threats are introduced. All existing security properties (cookie handling, error resilience, platform-aware persistence) are preserved exactly.
</threat_model>

<verification>
After both tasks complete:

1. **Build validation:**
   ```powershell
   dotnet build Ray.BiliBiliTool.sln --no-restore
   ```
   Must succeed with no errors in DailyTaskAppService.cs

2. **Characterization test validation:**
   ```powershell
   dotnet test test/Ray.BiliBiliTool.CharacterizationTests/ --filter "DailyTaskCharacterizationTests"
   ```
   Both tests must pass:
   - Daily_task_enabled_path_preserves_current_sequence_and_markers
   - Daily_task_multi_account_wrapper_continues_after_account_failure

3. **Integration test validation:**
   ```powershell
   dotnet test test/Ray.BiliBiliTool.Host.IntegrationTests/
   ```
   All host startup tests must pass

4. **Architecture test validation:**
   ```powershell
   dotnet test test/Ray.BiliBiliTool.ArchitectureTests/
   ```
   All boundary guardrails must pass

5. **Code review:**
   - Verify XML documentation completeness (class + all methods)
   - Verify inline comments clarify configuration-driven branching
   - Verify no functional changes (diff should show only doc additions)
   - Verify all Chinese comments preserved
   - Verify all TaskInterceptor attributes unchanged
</verification>

<success_criteria>
- DailyTaskAppService.cs has comprehensive XML documentation following Phase 3 pattern
- Six-step workflow (SetCookies → Login → GetStatus → WatchAndShare → AddCoins → ReceiveVipPrivilege) is clearly documented
- Configuration-driven behavior is explained via inline comments
- Platform-aware persistence routing (QingLong vs JSON) is documented
- Multi-account wrapper behavior is explained in class-level docs
- All characterization tests pass without modification
- All host integration tests pass
- All architecture tests pass
- Build succeeds with no warnings
- Git diff shows only documentation additions (no functional changes)
- TaskInterceptor attributes and diagnostic markers preserved exactly
- Chinese comments preserved exactly
</success_criteria>

<output>
After completion, create `.planning/phases/04-dailytask-refactor-slice/04-01-SUMMARY.md` following the template at `~/.copilot/get-shit-done/templates/summary.md`.

Include:
- Performance metrics (duration, files modified)
- Accomplishments (documentation coverage, test validation results)
- Decisions made (documentation approach, comment preservation strategy)
- Patterns established (application service refactor pattern replication)
- Requirements completed (FLOW-02)
- Next phase readiness assessment for Phase 5 (Scheduler Shell Cleanup)
</output>
