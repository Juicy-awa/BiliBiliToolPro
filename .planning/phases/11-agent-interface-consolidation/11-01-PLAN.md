---
phase: 11-agent-interface-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IApiApi.cs
autonomous: true
requirements:
  - IFACE-01
  - IFACE-02
must_haves:
  truths:
    - "IApiApi.cs exists with all 7 merged interfaces' methods organized in 5 #region sections"
    - "IUserInfoApi.cs is unchanged (critical: IWbiService dependency)"
    - "FollowingsOrderType enum and RelationApiConstant class are co-located in IApiApi.cs"
    - "Interface-level [Headers] covers Host only; per-group Referer headers move to method level"
  artifacts:
    - path: "src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IApiApi.cs"
      provides: "Merged api.bilibili.com interface"
      contains: "#region UpInfo"
  key_links:
    - from: "IApiApi"
      to: "api.bilibili.com"
      via: "[Headers(\"Host: api.bilibili.com\")]"
      pattern: "Host: api.bilibili.com"
---

<objective>
Create IApiApi.cs — a single Refit interface for all api.bilibili.com endpoints currently split across 7 interfaces. Organize by #region. Keep IUserInfoApi untouched.

Purpose: Current split (IUpInfoApi, IDailyTaskApi, IRelationApi, IChargeApi, IVideoApi, IVideoWithoutCookieApi, IArticleApi) is arbitrary — not aligned to the host boundary. One host = one interface, with #region to organize the surface area.

Output: IApiApi.cs with 5 #region sections (~25 methods total). Old interface files remain until Plan 02 deletes them.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Source interfaces being merged — read each before writing IApiApi.cs -->
@src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IUpInfoApi.cs
@src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IDailyTaskApi.cs
@src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IRelationApi.cs
@src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IChargeApi.cs
@src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IVideoApi.cs
@src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IArticleApi.cs

<!-- NOT merged — stays separate due to IWbiService circular dependency -->
@src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IUserInfoApi.cs
@src/Ray.BiliBiliTool.Agent/BiliBiliAgent/WridEncryptionDelegatingHandler.cs
@src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Services/WbiService.cs

<interfaces>
<!-- Critical: header inheritance rules when merging -->
<!-- IRelationApi had [Headers("Host: api.bilibili.com", "Referer: https://space.bilibili.com/")] at interface level -->
<!-- IVideoApi had [Headers("Host: api.bilibili.com")] at interface level -->
<!-- IUpInfoApi had [Headers("Referer: https://www.bilibili.com/", "Origin: https://www.bilibili.com", "Host: api.bilibili.com")] at interface level -->
<!-- IDailyTaskApi had [Headers("Host: api.bilibili.com")] at interface level -->
<!-- IChargeApi had [Headers("Host: api.bilibili.com")] at interface level -->
<!-- IArticleApi had [Headers("Host: api.bilibili.com")] at interface level -->
<!--
RULE: IApiApi only gets [Headers("Host: api.bilibili.com")] at the interface level.
All Referer/Origin headers that were at the source interface level MUST move to method level
so they apply only to the relevant methods, not the whole merged interface.
-->

<!-- IRelationApi: "Referer: https://space.bilibili.com/" was interface-level -->
<!-- After merge: each relation method must carry [Headers("Referer: https://space.bilibili.com/")] -->
<!-- (or use the existing method-level [Headers] block and add Referer to it) -->

<!-- IUpInfoApi: "Referer: https://www.bilibili.com/", "Origin: https://www.bilibili.com" were interface-level -->
<!-- After merge: GetSpaceInfo method must carry these explicitly -->

From src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Services/WbiService.cs:
```csharp
public class WbiService(ILogger<WbiService> logger, IUserInfoApi userInfoApi) : IWbiService
```
// WbiService depends on IUserInfoApi directly.
// WridEncryptionDelegatingHandler depends on IWbiService.
// If IUserInfoApi were merged into IApiApi (which has WridEncryptionDelegatingHandler),
// calling any IApiApi method would invoke WridEncryptionDelegatingHandler
// → IWbiService.GetWridAsync() → IApiApi.LoginByCookie() → WridEncryptionDelegatingHandler → infinite loop.
// Therefore IUserInfoApi MUST remain a separate interface registered with ignoreWrid=true.

From BiliHosts.cs:
```csharp
public const string Api = "https://api.bilibili.com";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IApiApi.cs with all merged methods and regions</name>
  <files>src/Ray.BiliBiliTool.Agent/BiliBiliAgent/Interfaces/IApiApi.cs</files>
  <action>
Create a new file IApiApi.cs in the Interfaces folder. Merge all methods from the 6 source files (IUpInfoApi, IDailyTaskApi, IRelationApi, IChargeApi, IVideoApi+IVideoWithoutCookieApi, IArticleApi) into a single interface.

Interface-level header: only `[Headers("Host: api.bilibili.com")]`.
No Referer or Origin at the interface level — these vary per region.

Organize using exactly these 5 #region sections in order:

**#region UpInfo**
- GetSpaceInfo — from IUpInfoApi
  - Method-level: `[Headers("Referer: https://www.bilibili.com/", "Origin: https://www.bilibili.com")]`
  - (These were interface-level in IUpInfoApi; must move to method level here)

**#region 每日任务**
- GetDailyTaskRewardInfoAsync — from IDailyTaskApi (has own Referer+Origin headers already)
- GetDonateCoinExpAsync — from IDailyTaskApi (has own Referer+Origin headers)
- ReceiveVipPrivilegeAsync — from IDailyTaskApi

**#region 关注**
ALL relation methods need explicit `"Referer: https://space.bilibili.com/"` added to their [Headers] block because that header was at IRelationApi's interface level and will be lost in the merge.
- GetFollowings: add `[Headers("Referer: https://space.bilibili.com/")]`
- GetFollowingsByTag: add Referer to its existing `[Headers("Cache-Control: no-cache", "Pragma: no-cache")]` → becomes `[Headers("Cache-Control: no-cache", "Pragma: no-cache", "Referer: https://space.bilibili.com/")]`
- GetTags: add Referer to its existing `[Headers("Sec-Fetch-Mode: no-cors", "Sec-Fetch-Dest: script")]` → becomes `[Headers("Sec-Fetch-Mode: no-cors", "Sec-Fetch-Dest: script", "Referer: https://space.bilibili.com/")]`
- CreateTag: add Referer to its existing `[Headers("Origin: https://space.bilibili.com")]` → becomes `[Headers("Origin: https://space.bilibili.com", "Referer: https://space.bilibili.com/")]`
- CopyUpsToGroup: same pattern as CreateTag
- ModifyRelation: same pattern as CreateTag

**#region 充电**
- Charge (Obsolete) — from IChargeApi (no extra headers needed)
- ChargeV2Async — from IChargeApi (has own Content-Type/Referer/Origin)
- ChargeCommentAsync — from IChargeApi (has own Content-Type/Referer/Origin)

**#region 视频**
- ShareVideo — from IVideoApi
- UploadVideoHeartbeat — from IVideoApi
- AddCoinForVideo — from IVideoApi (keep the `#region 投币相关` sub-region if desired, or fold flat)
- GetDonatedCoinsForVideo — from IVideoApi
- SearchVideosByUpId — from IVideoApi
- GetBangumiBySsid — from IVideoApi
- GetVideoDetail — from IVideoWithoutCookieApi (no cookie param, fold into same region)

**#region 专栏**
- SearchUpArticlesByUpIdAsync — from IArticleApi
- SearchArticleInfoAsync — from IArticleApi
- AddCoinForArticleAsync — from IArticleApi
- LikeAsync — from IArticleApi

After the interface closing brace, add the companion types that were in IRelationApi.cs:
```csharp
public enum FollowingsOrderType { ... }
public class RelationApiConstant { ... }
```
(Copy them verbatim from IRelationApi.cs)

Required using statements (collect from all source files):
- using System.ComponentModel;
- using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos;
- using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.Article;
- using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.Relation;
- using Ray.BiliBiliTool.Agent.BiliBiliAgent.Dtos.Video;
- using Refit;

Do NOT create sub-interfaces. Do NOT use inheritance. All methods are direct members of IApiApi.
  </action>
  <verify>
    <automated>dotnet build src/Ray.BiliBiliTool.Agent/Ray.BiliBiliTool.Agent.csproj --no-restore 2>&amp;1 | Select-String "error|warning|Build succeeded"</automated>
  </verify>
  <done>
    - IApiApi.cs exists and compiles (Agent project builds with 0 errors)
    - Interface has exactly 5 #region sections: UpInfo, 每日任务, 关注, 充电, 视频, 专栏
    - FollowingsOrderType enum and RelationApiConstant class are present after the interface
    - IUserInfoApi.cs is untouched
    - Old interface files (IUpInfoApi.cs etc.) still exist at this point — Plan 02 deletes them
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IApiApi → api.bilibili.com | All outbound HTTP — no new boundaries, existing trust model unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | WridEncryptionDelegatingHandler | accept | IUserInfoApi stays separate; circular dep prevented by design. Handler only activates when w_rid param is present. |
| T-11-02 | Spoofing | IApiApi Host header | accept | Host header set at interface level unchanged from source interfaces. No new attack surface. |
</threat_model>

<verification>
- `dotnet build` → 0 errors in Ray.BiliBiliTool.Agent project
- IApiApi.cs contains all 5 #region markers
- grep for "IUserInfoApi" in WbiService.cs still resolves to the unchanged IUserInfoApi
</verification>

<success_criteria>
- IApiApi.cs compiles cleanly with all ~25 methods from 7 source interfaces
- No behavioral changes — same HTTP verbs, paths, headers, and request/response types
- FollowingsOrderType and RelationApiConstant remain accessible from the Interfaces namespace
</success_criteria>

<output>
After completion, create `.planning/phases/11-agent-interface-consolidation/11-01-SUMMARY.md`
</output>
