---
phase: 16-web-composition-and-regression-verification
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified: []
autonomous: true
requirements: [WEB-06]
must_haves:
  truths:
    - "Solution builds without errors"
    - "Architecture tests pass including new Web component guardrail rule"
    - "Host integration tests pass including new workflow seam DI assertions"
    - "Web component tests pass (25 tests across Admin, Login, Schedules slices)"
  artifacts:
    - path: "test/Ray.BiliBiliTool.ArchitectureTests/Ray.BiliBiliTool.ArchitectureTests.csproj"
      provides: "Architecture test suite — 4 facts green including new Web.Components rule"
    - path: "test/Ray.BiliBiliTool.Host.IntegrationTests/Ray.BiliBiliTool.Host.IntegrationTests.csproj"
      provides: "Host integration test suite — Web_startup test green with 6 new assertions"
    - path: "test/Ray.BiliBiliTool.Web.ComponentTests/Ray.BiliBiliTool.Web.ComponentTests.csproj"
      provides: "Web component test suite — 25 tests green"
  key_links:
    - from: "Plan 16-01 output"
      to: "dotnet test results"
      via: "CI regression run"
      pattern: "通过|Passed"
---

<objective>
Run the full WEB-06 regression suite: build, architecture tests, host integration tests, and Web component tests. Report results.

Purpose: Confirm the milestone's boundary work is structurally sound and all observable behaviors are preserved (WEB-06).
Output: All 4 test suites green; milestone ready to close.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-web-composition-and-regression-verification/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full WEB-06 regression suite</name>
  <files></files>
  <action>
Run the four test suites in sequence. Report pass/fail counts for each.

**Step 1 — Solution build:**
```powershell
dotnet build Ray.BiliBiliTool.sln --no-restore -v minimal 2>&1 | Select-String "error|warning" | Select-Object -First 10
```
Expected: no errors.

**Step 2 — Architecture tests (includes new Web.Components guardrail):**
```powershell
dotnet test test/Ray.BiliBiliTool.ArchitectureTests/Ray.BiliBiliTool.ArchitectureTests.csproj 2>&1 | Select-Object -Last 5
```
Expected: all facts pass including `Web_component_code_behind_classes_should_not_directly_depend_on_infrastructure`.

**Step 3 — Host integration tests (includes new workflow seam DI assertions):**
```powershell
dotnet test test/Ray.BiliBiliTool.Host.IntegrationTests/Ray.BiliBiliTool.Host.IntegrationTests.csproj 2>&1 | Select-Object -Last 5
```
Expected: `Web_startup_boots_and_exposes_critical_services` passes with all 11 assertions (5 original + 6 new).

**Step 4 — Web component tests:**
```powershell
dotnet test test/Ray.BiliBiliTool.Web.ComponentTests/Ray.BiliBiliTool.Web.ComponentTests.csproj 2>&1 | Select-Object -Last 5
```
Expected: 25 tests pass (9 Admin + 3 Login + 13 Schedules).

**If any suite fails:** Read the error output, diagnose, fix, and re-run the failing suite before reporting. Do not leave any red test suite unresolved.
  </action>
  <verify>
    <automated>dotnet test test/Ray.BiliBiliTool.ArchitectureTests/Ray.BiliBiliTool.ArchitectureTests.csproj 2>&1 | Select-Object -Last 3 ; dotnet test test/Ray.BiliBiliTool.Host.IntegrationTests/Ray.BiliBiliTool.Host.IntegrationTests.csproj 2>&1 | Select-Object -Last 3 ; dotnet test test/Ray.BiliBiliTool.Web.ComponentTests/Ray.BiliBiliTool.Web.ComponentTests.csproj 2>&1 | Select-Object -Last 3</automated>
  </verify>
  <done>All 4 suites report 0 failures. WEB-06 regression checks pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → file system | Test commands read project files — no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Information Disclosure | Test output | accept | Test output is local only; no secrets in test assertions. |
</threat_model>

<verification>
All four commands exit with code 0 and report 0 failures.
</verification>

<success_criteria>
- `dotnet build Ray.BiliBiliTool.sln` — 0 errors
- Architecture tests — all facts green (4 rules)
- Host integration tests — Web_startup test green (11 total assertions)
- Web component tests — 25 tests green
- WEB-06 milestone requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/16-web-composition-and-regression-verification/16-02-SUMMARY.md`
</output>
